# HawkStars — Application Audit

**Date:** 2026-09-14 · **Revised:** 2026-09-15 — re-rated against the documented trust model, then re-verified against `bc908c9e` plus the working tree. **16 findings verified fixed and removed**; 3 regressions introduced by those fixes are now recorded as findings (QUA-32, SEC-13, I18N-4).
**Scope:** Full independent sweep — security, performance & caching, code quality / CI / deploy, SEO, i18n, accessibility.
**Baseline:** working tree as it stands on disk. First pass: `main` at `a12ebf95` plus 14 uncommitted changes. Re-verification (2026-09-15): `main` at `bc908c9e` plus 9 uncommitted changes. ~52,100 lines across `app/`, `components/`, `lib/`, `payload/`, `utils/`, `i18n/`, `types/`; 673 TS/TSX files; 45 page-builder blocks.

## Method

This audit was built **from the current source only**. The existing `AUDIT.md` (2026-09-10) was excluded from the tree handed to every analysis pass, no prior finding IDs were carried over, and no prior conclusion was reused. For full transparency: its executive summary was read at the outset while scoping this work, which is why the "Fixed since the previous pass" section below can exist at all — but no analysis pass saw it, and every finding here was independently derived and then independently verified against the code. Where a finding here matches an old one, the code produced it again independently — and several old findings are *absent* here because they are genuinely fixed (see "Fixed since the last pass").

Four parallel passes read the code directly. Every finding carries a `file:line` that was actually opened. Twenty-six of the highest-severity claims were then re-verified by hand in a second pass (marked ✓) — including parsing `deploy.yml` with a YAML loader to check the trigger structure, reading the `Contribution` field list against the component that receives it, and character-by-character inspection of the JSON-LD escape.

### Toolchain — actually executed

| Command | Result |
| --- | --- |
| `tsc --noEmit` (working tree) | ❌ **1 error** — `utils/metadata.ts:54`. Was 0 on 2026-09-14. See **QUA-32**. |
| `eslint .` (working tree) | ❌ **5 errors, 5 warnings — exit 1.** Errors all in `components/ui/map.tsx`, unchanged since the first pass. See **QUA-30**. |
| `prettier --check .` | ✅ clean — "All matched files use Prettier code style!" |
| `vitest run` | ⚠️ not run — `node_modules` was installed on macOS; the shell available for this audit is linux-aarch64 and rolldown's native binding does not resolve. **The test suite's real status remains unverified.** CI can now at least launch a browser for it (QUA-12 closed), which it could not before. |
| `next build` | ⚠️ not run — same reason. It would fail regardless: `next build` typechecks, and `tsc` is red (QUA-32). Nothing else runs it either (QUA-2). |

**Two of the three `check` steps now fail.** `eslint .` exits 1 (unchanged since the first pass) and `tsc --noEmit` has gone from 0 errors to 1 — introduced by one of the fixes made since. Neither is visible from reading the source; both took running the tool. `pnpm build` fails for the same reason as the typecheck, so the site cannot currently be deployed. Details in QUA-30 and QUA-32.

## Trust model

*Added 2026-09-15, after the first pass. Severities below are rated against this model; the findings it changed are marked **[re-rated]**.*

**Every Payload account belongs to a person associated with the NGO.** There is no public sign-up, no member self-registration and no end-user account system — accounts exist only because an admin created one (`Users.access.create: authenticatedAdmin`). Payload is a workplace tool for NGO staff and volunteers, not a customer-facing application. Three tiers:

| Tier | Flags | Intended scope |
| --- | --- | --- |
| **Normal** | neither | Basic account — sign in and view content in `/admin`. No create, update, delete or publish. |
| **Editor** | `isEditor` | Manages content: pages, news, projects, events, artworks, curators, media, partners. Not users, settings or financial records. |
| **Admin** | `isAdmin` | Full control. |

This is now written up in `.claude/access-control.md`, which is the canonical version.

What it changes about the findings below:

- **A logged-in user is not an anonymous attacker.** Findings originally phrased as "any authenticated user can do X" are not remotely exploitable by the public, and several have been rated down accordingly.
- **It does not make access control optional.** Three reasons survive the trust assumption, and they are the ones the ratings now reflect: *accidental* damage by someone who was handed a button they should not have had; *account compromise* (a phished volunteer holds a 30-day `Lax` session, and `'unsafe-inline'` is in the CSP, so one bad login is a site-wide problem); and *data minimisation* for donor and subscriber personal data, which under GDPR applies inside the organisation as much as outside it — and which the code already states as intent at `Contribution/index.ts:18`.
- **The public bar is unchanged.** Anything reachable without a session — the frontend, the REST/GraphQL surface, anything serialised into a page — is held to exactly the same standard as before. SEC-2, SEC-3 and SEC-8 are untouched by the trust model, because none of them requires an account.
- **One finding gets more serious, not less.** The tiers described above are *not implemented*: the Normal tier currently has the same content powers as an Editor. That is now the headline access-control gap (SEC-10), promoted from Medium to High — it is the difference between the model you intend and the model you have.

---

## Executive summary

This is a well-engineered codebase and the list below is long because the sweep was thorough, not because the work is poor. `strict: true` with no escape hatches, zero `@ts-ignore`/`@ts-expect-error`, seven explicit `any` in the whole tree (five of them unavoidable module declarations), zero `console.log`, two TODOs, a 45-block page builder whose slugs and renderer registry are an *exact* 1:1 set match, and translation files with **perfect key parity** across 18 namespace pairs and 900+ keys. Unusually, the code is commented with its own reasoning — `nginx.conf` explains its cache-lock strategy, `utils/rateLimit.ts` documents the `X-Forwarded-For` bypass it fixes, `app/sitemap.ts` explains exactly which collections carry `_status` and why. A striking number of findings below exist because that reasoning was written down in one place and then not applied in the next one over.

Six things need attention now. The donor-PII leak that led this list on 2026-09-14 has been verified fixed and removed — see **Fixed and verified closed** below.

**0. Two of the three CI gates are red.** `eslint .` exits 1 with 5 errors, all in `components/ui/map.tsx` and all unchanged since the first pass (refs read during render, setState in effects, a mutation of Leaflet's shared `L.drawLocal`). And `tsc --noEmit` has gone from 0 errors to 1: closing SEO-3 added `crowdfunding` and `gaming` to `SITE_GET_URLS` without adding matching keys to `metadata.json`, so `utils/metadata.ts:54` no longer compiles. `pnpm build` fails on the same error, so this is not just a CI annoyance — nothing can ship. QUA-30, QUA-32. ✓

**1. Unpublished content is publicly readable.** `pages`, `news` and `hawk_projects` all set `read: anyone`/`() => true` with `versions.drafts` + autosave enabled, and `findBySlug` (`lib/payload/queries/helpers.ts:26`) still filters on `slug` alone. `draft: false` does *not* exclude a document whose `_status` is `draft`. An editor's in-progress page is live at its public URL as they type, and `GET /api/news` lists the drafts directly. Unchanged since the first pass, and the sitemap half (SEO-2) is unchanged too — `hawk_projects` and `news` carry the `_status` filter, `pages` still does not. ✓

**2. The deploy takes the site down *before* it knows the build works, and nothing checks that it came back up.** `deploy.yml:161-165` runs `pm2 stop` → `pm2 delete` → `rm -rf .next`, and only then installs and builds. `set -e` means any failure exits with the app deleted from PM2 and `pm2 save` never reached, so even a reboot resurrects nothing. The `check` job still runs no `pnpm build`, and `app/api/health/route.ts` — whose docstring says it exists so the workflow can confirm the process is serving before calling the deploy successful — is still never called. Given point 0, the next push to `main` walks into exactly this. ✓

**3. Two production secrets never reach the server.** `deploy.yml:87-88` exports `PERSONAL_GITHUB_TOKEN` and `NEXT_PUBLIC_CARTO_BASEMAP_KEY` into the step's `env:`, but the ssh-action `envs:` whitelist on line 98 lists 19 names and neither of those is among them. The Carto basemap key is written empty into production's `.env` on every deploy. Still a two-word fix. ✓

**4. A cache keyed on nothing serves the wrong language.** `payload/components/RichText/index.tsx:173` still does `blockConvertersCache ??= …` inside a function that takes `lng` — the parameter is ignored after the first call. In a long-lived PM2 process, whichever locale renders a rich-text block first wins until restart, and every block on every `/en/…` page then receives `lng='pt'`. The file was touched since the first pass but not here. ✓

**5. The three permission tiers do not exist in the code.** All six content collections still use `create/update/delete: authenticated`, which grants to every account including one with no role, and `Users.access.admin` is still `authenticated`. `isEditor` grants nothing an account does not already have. SEC-20 means the role checkboxes still cannot be set through the admin UI, so the tier could not be granted deliberately even if the collections were fixed — these two need to go in together. ✓

Nothing else rated Critical. The payment and public-submission flows are genuinely well defended — rate-limited, zod-validated, `is_confirmed` forced false at four independent layers, the EasyPay webhook using `timingSafeEqual` and failing *closed* when its secret is missing, with tests covering all of it.

**Counts: 0 Critical · 25 High · 45 Medium · 24 Low — 94 open.** Down from 109. Sixteen findings were verified fixed and removed on 2026-09-15; three regressions introduced by those same fixes were added (QUA-32, SEC-13, I18N-4), and SEC-6, QUA-13 and QUA-31 were rewritten because they were partially addressed. Every removal was confirmed by reading the current code, not by reading a commit message.

### Fix in this order

| # | Item | Effort | Why this position |
| --- | --- | --- | --- |
| 0 | **QUA-32** — add `crowdfunding`/`gaming` keys to `metadata.json` (both locales) | 15 min | Typecheck *and* build are failing; nothing ships until this is green |
| 1 | **QUA-30** — the 5 lint errors in `components/ui/map.tsx` | 1 h | The other red gate |
| 2 | **SEC-13** — `!(isEditor \|\| isAdmin)`, not `!isEditor \|\| !isAdmin` | 5 min | A fix that locked out everyone; one-line revert of the logic |
| 3 | **SEC-2 / SEO-2** — `_status: published` in collection access, `findBySlug`, and the sitemap's `pages` find | 1 h | Drafts are public and being indexed |
| 4 | **SEC-3** — confirm the production `PAYLOAD_SECRET` is not the 7-character dev value; rotate | 20 min | Still 7 lowercase characters in `.env` |
| 5 | **QUA-1 + QUA-2 + QUA-4** — build in CI; build before stopping PM2; poll `/api/health` | 3 h | With two gates red, the next deploy is the dangerous one |
| 6 | **QUA-6** — two names appended to the `envs:` list | 2 min | A production key is still empty |
| 7 | **QUA-5** — key the block-converter cache by locale | 15 min | EN pages are silently serving PT links |
| 8 | **SEC-10 + SEC-20** — `authenticatedEditor`/`authenticatedAdmin` on the six collections; fix the role-checkbox `condition` | 2 h | The three tiers do not exist in code, and the roles cannot be granted — do these together |
| 9 | **I18N-4** — restore the member-project date formatter | 20 min | Dates render blank today; also clears a lint warning |
| 10 | **SEC-6** — `update: authenticatedEditor` on the `mainPage` global | 2 min | Last of the three globals; the other two are done |
| 11 | **SEC-11** — set `EASYPAY_WEBHOOK_SECRET`; make the authorisation handler idempotent | 1 h | Donations are either never confirmed, or double-counted |
| 12 | **PERF-5** — raise `autosave` from 100 ms; guard the revalidate hooks on `_status` | 30 min | ~10 site-wide cache wipes per second while anyone edits |
| 13 | **PERF-1 / PERF-9** — invalidation hooks for the 4 globals; add the Pages tag | 2 h | Editors publish and see nothing change for an hour |
| 14 | **A11Y-2 / A11Y-3** — donation-form error messaging; slideshow pause control | 3 h | WCAG Level A, on a public-funded EU NGO site |
| 15 | **SEO-1** — `notFound()` in the catch-all route | 30 min | Every stale deep URL is an indexable 200 |
| 16 | **PERF-3 / PERF-4** — kill the `react-icons/lu` namespace import; lazy-load client blocks | 4 h | Largest single lever on JS shipped per page |
| 17 | **I18N-1** — single-link nav items are dead buttons on mobile | 20 min | Navigation is broken on phones |

### Fixed and verified closed

Removed from this report on 2026-09-15. Each was confirmed by reading the current code — the evidence is listed so you can re-check any of them.

| ID | Was | Verified by |
| --- | --- | --- |
| **SEC-1** | Critical | `contribution.ts:29-35` now passes `select` (no `extra_info`, `transaction_key`, `easypay_id`, `payment_method`), and `contribute/page.tsx:51-54` maps `donor: item.is_anonymous ? '' : item.donor` **before** the client boundary. Both halves closed. |
| **SEC-4** | Medium | `Notification/index.ts:22-27` — all four operations now `authenticatedAdmin`. The log text still embeds donor name and amount, but only admins can read it, and admins can read `contributions` anyway. |
| **SEC-5** | Medium | `Contribution/index.ts:28` and `BoardMember.ts:24` both declare `delete: authenticatedAdmin`. |
| **SEC-9** | Medium | All three list routes now use `Number.isInteger` + `Math.min(Math.max(…, 1), 50)` for `limit` and a 1–9999 clamp for `page`. `?limit=` no longer yields `limit: 0`. |
| **SEO-3** | Medium | `utils/paths.ts:28-29` adds both routes to `SITE_GET_URLS`, `:102-103` adds them to `routes` with priorities. *(Introduced QUA-32 — see above.)* |
| **PERF-8** | Medium | `curator/[slug]/page.tsx:29-30` — `cacheLife('hours')` and `cacheTag(\`curators:${slug}\`, 'curators')` are uncommented. |
| **I18N-2** | Medium | `team.ts:18` takes `lng: Language` and `:28` passes `locale: lng`; because `lng` is a parameter of the `'use cache'` function it is part of the cache key. |
| **QUA-11** | Medium | `utils/paths.ts:61` now builds from `BASE_URL`, which carries the `|| 'https://hawkstars.org'` fallback. |
| **QUA-12** | Medium | `pnpm-workspace.yaml` adds `playwright: true` to `allowBuilds`, and `deploy.yml:42` runs `pnpm exec playwright install --with-deps chromium`. |
| **QUA-15** | Medium | `deploy.yml:202` — `always()` removed; the `storybook` job now inherits the `check` gate. |
| **PERF-23** | Low | `'use client'` removed from `MultiRowImage`, `BentoGridBlock` and `AccordionBlock`. |
| **PERF-24** | Low | `artwork.ts:16` — `limit: 1` on the single-artwork lookup. |
| **QUA-19** | Low | `lib/payload/seed/` no longer exists. |
| **QUA-24** | Low | The `response.status === 190` branch is gone from `app/api/instagram/route.ts`. |
| **QUA-27** | Low | Renamed to `totalContributionValueQuery.ts`, imports updated. |
| **I18N-8** | Low | `LanguageSwitcher.tsx:40`, `:56-59` — `useSearchParams()` is read and appended to the target href. |

## Security

### [High] SEC-2 — Unpublished drafts of Pages, News and Projects are publicly readable ✓
- **Where:** `payload/collections/Pages/index.ts:21` (`read: () => true`), `payload/collections/News/index.ts:48` (`read: anyone`), `payload/collections/HawkProject/index.ts:61` (`read: anyone`); drafts enabled at `Pages/index.ts:163`, `News/index.ts:101`, `HawkProject/index.ts:213`; public read path `lib/payload/queries/helpers.ts:26` — `const where: Where = { slug: { equals: slug } };` — and `:35` `draft: opts?.preview ?? false`
- **What:** All three collections enable `versions.drafts` with autosave, so every document carries `_status`. No public read path filters on it. `draft: false` only means "do not resolve the newest draft *version*"; it does not exclude a main document whose `_status` is `'draft'`. So `findPublishedBySlug` returns drafts despite the name, the list queries include them, and the REST API serves them directly: `GET /api/pages?where[_status][equals]=draft`.
- **Why it matters:** Embargoed announcements, unfinished project pages and press releases are readable by anyone who guesses a slug or simply lists the collection. With autosave at a 100 ms interval (PERF-5), a draft is live at its public URL while the editor types. The correct pattern is already used in three other places (`app/sitemap.ts:35`, `lib/payload/client-side/queries/news.ts:36`, `…/projects.ts:19`), and the authenticated preview routes that drafts were *meant* to be reached through are correctly gated.
- **Fix:** Replace `read: anyone` with `({ req: { user } }) => user ? true : { _status: { equals: 'published' } }` on all three, and add the same clause to the `where` in `helpers.ts:26` and `news.ts:34` / `projects.ts` when `preview` is false, so the rendered pages are correct independently of access control.

### [High] SEC-3 — `PAYLOAD_SECRET` in the working tree is 7 lowercase letters ✓
- **Where:** `.env` (value not reproduced — it is 7 characters, all `[a-z]`); consumed at `payload.config.ts:63`, `:213`; production value comes from a GitHub secret at `deploy.yml:69`, `:132`
- **What:** This is the HMAC key Payload uses to sign admin session JWTs. `payload.config.ts:63` asserts only that it is *present*, never that it has entropy.
- **Why it matters:** The keyspace is 26⁷ ≈ 8×10⁹ — recoverable offline in minutes from a single captured token, after which anyone can mint a JWT for any user id, including a full admin. This particular `.env` points at `mongodb://localhost:27017` and `api.test.easypay.pt`, so it reads as a developer file; the risk is that the same string was pasted into the production `PAYLOAD_SECRET` secret. That cannot be checked from the repository.
- **Fix:** Confirm the production secret, and rotate it regardless: `openssl rand -base64 48`. Add a boot assertion beside the others at `payload.config.ts:62-73` that the secret is ≥32 bytes.

### [Medium] SEC-6 — The `mainPage` global can still be rewritten by the Normal tier ✓ **[partially fixed 2026-09-15]**
- **Where:** `payload/globals/MainPage/config.ts:34-37` — `read: authenticated`, `update: authenticated`
- **Fixed:** `payload/globals/Header/config.ts:17` now declares `update: authenticatedEditor`, and `payload/globals/NewsList/config.ts:31` likewise. The site-wide navigation half of this finding is closed.
- **Still open:** `MainPage` is the homepage global — every hero, block and section on the highest-traffic route in the site — and it is the last of the three to still accept writes from any account, including one with no role. It was named in the original fix note and missed.
- **Fix:** `update: authenticatedEditor` on `payload/globals/MainPage/config.ts:36`, matching the two that were done.

### [Medium] SEC-7 — CMS-supplied URLs reach `<iframe src>` and `<a href>` with no scheme check
- **Where:** `components/projects/single/ProjectsSingleHero.tsx:155-156`; validator at `payload/collections/HawkProject/HawkProjectPageTab.ts:96-120`; `components/utils/HawkLink/ExternalHawkLink.tsx:19`; link field with no `validate` at `payload/fields/link.ts:100-111`
- **What:** The `videoUrl` validator constrains the value only if it *already* contains `youtube.com`/`youtu.be`; anything else falls through to `return true` at `HawkProjectPageTab.ts:116`. So `javascript:…` is accepted and rendered as an iframe source. `ExternalHawkLink` renders any `link.url` verbatim as an anchor `href`, and the link field carries no validator at all.
- **Why it matters:** Held at Medium under the trust model. The author is a trusted NGO person, so this is not "an attacker plants XSS" — the realistic vectors are a **compromised account** (30-day `Lax` session, `Users/index.ts:33`) and an editor pasting a URL they did not vet. Either way the payload lands on *public, unauthenticated visitors*, which is the bar the trust model does not relax. CSP does not help: `next.config.ts:30` includes `'unsafe-inline'` in `script-src`, which governs `javascript:` navigation, so it executes same-origin — including against an admin browsing the site while logged into `/admin`. The right helper already exists — `utils/paths.ts:121` `isHttpUrl`, applied to member submissions via `validateProjectUrl.ts` — it is simply not applied here. One note that *does* change: `hawk_projects` is `create/update: authenticated`, so today the Normal tier can plant this; fixing SEC-10 narrows the surface to editors but does not remove it.
- **Fix:** `validate: (v) => !v || isHttpUrl(v) || 'Must start with http(s)://'` on `HawkProjectPageTab.ts:96` and on `payload/fields/link.ts:100`; guard `ExternalHawkLink.tsx:19`. For the iframe, use a provider allowlist the way `components/Crowdfunding/VideoEmbed.tsx:18-34` already does.

### [Medium] SEC-8 — The login rate limiter runs on `beforeLogin`, which does not fire for failed logins
- **Where:** `payload/collections/Users/hooks/rateLimitLogin.ts:14-27`, wired at `payload/collections/Users/index.ts:49`
- **What:** The hook throws 429 after 10 attempts per IP per 5 minutes, and its comment (echoed in `ONBOARDING.md:60`) describes it as capping attempts "regardless of which account is targeted". Payload runs `beforeLogin` *after* the password has been verified — a wrong password throws `AuthenticationError` before the hook chain is reached.
- **Why it matters:** The control documented as the defence against credential spraying never executes on the attempts it is meant to block. What remains is the per-account lockout (`maxLoginAttempts: 5`, 24 h `lockTime`), which does nothing against one attempt each against 500 addresses. nginx's `limit_req … burst=60` is far too loose to matter.
- **Fix:** Move the limiter in front of the operation — a `beforeOperation` hook checking `operation === 'login'`, or an nginx `limit_req` zone scoped to `location = /api/users/login`. Confirm Payload's hook ordering before rewriting (see *Not verified*).

### [High] SEC-10 — The three-tier model is not implemented: the Normal tier has full Editor powers ✓ **[re-rated: Medium → High]**
- **Where:** `payload/collections/Users/index.ts:15` (`admin: authenticated`); `Pages/index.ts:19-22`, `News/index.ts:49-51`, `HawkProject/index.ts:62-64`, `HawkEvent/index.ts:38-40`, `ArtCollection/index.ts:18-20`, `Curator/index.ts:17-19` — all `create/update/delete: authenticated`. The pattern is taught at `.claude/rules/frontend/payload.md` (now corrected).
- **What:** `isAdmin` and `isEditor` exist and are applied correctly by `Media`, `Documents`, `Partner`, `Sponsor` and `MemberProject` — but six content collections plus three globals gate writes on `authenticated`, which grants to all three tiers. A brand-new account with neither flag can create, edit, **publish** and **delete** pages, news, projects, events, artworks and curators, and can enter `/admin`.
- **Why it matters:** Promoted to High — not because of outside attackers, but because this is the gap between the permission model the organisation intends and the one the code enforces. The intended Normal tier is read-only; in practice `isEditor` currently grants nothing that an account does not already have, which makes the flag decorative and makes "grant someone Editor" a no-op. Everything that follows from a tiered model — onboarding a new volunteer safely, limiting what a compromised account can reach, being able to say who could have changed a page — does not hold today. Two contributing causes are worth fixing at the same time: the "typical pattern" block in the project's own Payload rules used `create/update/delete: authenticated` as the recommended default (corrected 2026-09-15), and SEC-20 means the role checkboxes cannot be set through the admin UI anyway.
- **Fix:** On those six collections — `admin: authenticatedEditor`, `create`/`update`: `authenticatedEditor`, `delete`: `authenticatedAdmin`. Set `Users.access.admin` to `authenticatedEditor` so a Normal account cannot enter the panel, **or** keep `authenticated` if Normal is meant to sign in and browse read-only — in which case the six collections above are what actually enforce read-only, so they must be fixed first either way. Fix SEC-20 in the same change, or the roles cannot be granted. `Users.access.read: authenticated` is fine under this model (colleagues seeing colleagues) and is no longer reported as an issue.

### [Medium] SEC-11 — EasyPay webhook: the secret is absent from the environment, and authorisation notifications are not idempotent ✓
- **Where:** `app/api/easypay/route.ts:29-52`, `:133-158`, `:176-208`; `.env` has no `EASYPAY_WEBHOOK_SECRET`; `.env.example:29`; `deploy.yml:86`, `:150`
- **What:** `isAuthorisedWebhook` is correct — it fails **closed** when the secret is unset and compares with `timingSafeEqual` after a length check. But the variable is simply missing from the working `.env`, so in that environment every EasyPay callback 401s. Separately, `handleAuthorisationNotification` unconditionally `payload.create`s a contribution on every authorisation notification with no lookup on `notification.key`, while `POST` deliberately returns `200` on every error path so retries are absorbed silently. The sibling path `updateContributionStatus` (`:184`) *does* query by `transaction_key` first.
- **Why it matters:** With the secret unset, no donation is ever marked `is_confirmed`, so `/transparency`'s public total and the donor wall permanently under-report — a reconciliation failure visible only as a Sentry warning. With it set, a retried or duplicated callback inserts a **duplicate** `Contribution`, and since the public total sums `value` over confirmed rows, the published figure over-reports. Both directions are wrong on a page whose entire purpose is financial transparency.
- **Fix:** Set `EASYPAY_WEBHOOK_SECRET` in every environment and assert it at boot alongside the others. In `handleAuthorisationNotification`, `payload.find({ where: { transaction_key: { equals: notification.key } }, limit: 1 })` first and `update` when a row exists. Add a unique index on `transaction_key`.

### [Medium] SEC-12 — CSP `script-src` allows `'unsafe-inline'`, which is what makes SEC-7 exploitable
- **Where:** `next.config.ts:28-45`, specifically `:30`
- **What:** `script-src 'self' 'unsafe-inline'` (plus `'unsafe-eval'` in dev), no nonce, no hashes. `frame-src` includes a bare `*.google.com`; `img-src` allows all of `https:`. The trade-off is documented in the file (nonces are incompatible with PPR / `cacheComponents`) and the reasoning is sound as far as it goes.
- **Why it matters:** The policy provides no XSS containment for the defects that actually exist here. `'unsafe-inline'` permits `javascript:` URL navigation, so SEC-7 executes.
- **Fix:** Given nonces are off the table, accept that CSP is not the mitigation and fix the injection sinks directly. Separately, tighten `frame-src` to the exact Google hosts embedded and narrow `img-src` from `https:` to the four hosts already listed at `next.config.ts:63-78`.

### [High] SEC-13 — The `/api/translate` guard now requires **both** role flags, locking out every admin and every editor ✓ **[regression introduced 2026-09-15]**
- **Where:** `payload/endpoints/translate.ts:22-23`
- **What:** The check was changed from `if (!user)` to:
  ```ts
  if (!user || !user.isEditor || !user.isAdmin)
    return Response.json({ error: 'Unauthorized' }, { status: 401 });
  ```
  Read the boolean: it rejects when `isEditor` is false **or** when `isAdmin` is false, so it only lets through a user who has *both* flags set. Under the documented model `isAdmin` implies editor and admins carry only `isAdmin` — so an admin is rejected, an editor is rejected, and a Normal user is rejected. The endpoint is now unreachable for everyone except an account someone happened to tick both boxes on.
- **Why it matters:** This is the direction-of-travel error that static review is worst at catching and that the original finding invited — the fix tightened the check but inverted the intent. The Translate view is still wired into the admin UI via `afterNavLinks`, so editors see a nav link that 401s. The docstring at `:16` still says "Requires an authenticated admin user", which now matches neither the code nor the apparent intent.
- **Fix:** `if (!user || !(user.isEditor || user.isAdmin))` — or import and use `authenticatedEditor`, which is exactly this predicate and already exists. Then make the docstring say "editor or admin". Note the endpoint is *also* non-functional in production for an unrelated reason (QUA-9), so test both before calling it done.

### [Low] SEC-14 — `BreadcrumbJsonLd` is the one JSON-LD builder without the `<` escape
- **Where:** `components/seo/JsonLd.tsx:84`
- **What:** `dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}` — every sibling (`:61`, `:118`, `:173`, `:225`) appends `.replace(/</g, '\\u003c')`. Checked character by character: in JS source `'\\u003c'` is the six literal characters `<`, a valid JSON string escape for `<`, so the escape in the other four **is** correct and does block `</script>` breakout.
- **Why it matters:** Not currently exploitable — `BreadcrumbJsonLd` and `WebPageJsonLd` have zero call sites. But `items[].name` is exactly the kind of value that comes from a CMS title, so the first caller added gets a breakout for free.
- **Fix:** Add the same escape, or extract one shared `<JsonLdScript schema={…}>` so it cannot be forgotten again.

### [Low] SEC-15 — Newsletter subscriber list readable and deletable by the Normal tier **[re-rated: reframed, severity held]**
- **Where:** `payload/collections/NewsletterSubscriber.ts:32-38` — all five operations are `authenticated`
- **Why it matters:** The collection's own header comment says it "stays locked to admins/editors, same pattern as `MemberProject`" — but `MemberProject` uses `authenticatedEditor`/`authenticatedAdmin` and this does not, so the code does not do what its comment claims. Under the trust model the exposure is data minimisation plus the ability to delete the list by accident, not exfiltration by a stranger — which is why it stays Low. The public *write* path is fine: rate-limited, zod-validated, duplicate errors swallowed so signup does not leak whether an address is already subscribed.
- **Fix:** `read/update: authenticatedEditor`, `create/delete: authenticatedAdmin` — which is what the comment already promises.

### [Low] SEC-16 — Google Drive folder name is interpolated into a Drive query string
- **Where:** `lib/google-drive/adapter.ts:29-33`, reached from `handleUpload` at `:53-55`
- **What:** `` q: `name='${folderName}' and mimeType='…' and trashed=false` `` with no escaping of `'`.
- **Why it matters:** Stays Low, and the trust model lowers it further in practice: the input comes from an editor, so the realistic failure is not an attack but a folder name containing an apostrophe (`Projeto d'Arte`) breaking the query or silently matching the wrong folder. Worth fixing as a correctness bug more than a security one.
- **Fix:** Escape backslashes and quotes, and validate the field against `^[\w \-]{1,64}$`.

### [Low] SEC-17 — Cloudinary upload failures are swallowed, leaving Media rows pointing at nothing
- **Where:** `lib/cloudinary/adapter.ts:44-46`
- **What:** `handleUpload` wraps the upload in `try { … } catch (err) { Sentry.captureException(err); }` and returns normally, so `file.filename`/`mimeType`/`filesize` are never reassigned.
- **Why it matters:** Payload then creates the `media` document as though the upload succeeded — a broken asset referenced across public pages, with only a Sentry event to show for it, on the site's only image pipeline.
- **Fix:** Re-throw after capturing so Payload aborts the create.

### [Low] SEC-18 — Seed creates a known-credential admin outside production
- **Where:** `payload/seed.ts:26-46`, invoked from `payload.config.ts:276-279`
- **What:** `onInit` returns early when `NODE_ENV === 'production'`, and production is pinned by `ecosystem.config.cjs:28` and `deploy.yml:130` — so production is safe today. Every other environment gets `admin@hawkstars.org` / `changeme123` with `isAdmin: true`.
- **Why it matters:** A staging box, preview deploy or reachable dev instance is a full compromise, and `payload/plugins/index.ts:80` disables TOTP in development while `payload.config.ts:223` disables email, so there is no second factor and no reset path.
- **Fix:** Gate on an explicit `SEED_DEV_DATA=true` rather than on `NODE_ENV`, and generate the password randomly, printing it once.

### [Low] SEC-19 — MongoDB runs without authentication
- **Where:** `.env:5` (`mongodb://localhost:27017/hawkstars`, no credentials); `deploy.yml:133` writes the same URI into production's `.env`; `db-backup.yml:86` runs `mongodump` with no auth
- **Why it matters:** Every process on the VPS, and anything achieving even limited code execution there, has unauthenticated read/write on donations, users and subscribers. A `mongod` bind-address misconfiguration turns straight into remote database access with no second gate.
- **Fix:** Enable authorisation, create a least-privilege application user, and move the credentials into the `DATABASE_URI` secret rather than the literal in `deploy.yml:133`.

### [Medium] SEC-20 — The role checkboxes cannot be set through the admin UI, so the tier model cannot be administered ✓ **[re-rated: Low → Medium]**
- **Where:** `payload/collections/Users/index.ts:67`, `:79` — `condition: (data, { isAdmin }) => isAdmin === true`
- **What:** Payload's signature is `(data, siblingData, { user })`. The second positional argument destructured here is `siblingData` — for a top-level field, the document being edited. So `isAdmin`/`isEditor` are shown based on whether *the record being edited* already has `isAdmin`, not on who is viewing. On the new-user form the record is empty, so both checkboxes are hidden; on an existing non-admin user they are hidden too. `ONBOARDING.md:60` documented the intended (and incorrect) behaviour as fact — corrected 2026-09-15.
- **Why it matters:** Promoted to Medium now that the tier model is explicit. It is not an access hole — `create`/`update` are `authenticatedAdmin`, so nobody is granted anything they shouldn't be — but it is the mechanism by which the model is *administered*, and it does not work. An admin cannot promote a Normal user to Editor through the UI; the flag has to be set directly in the database or via the API. That is a substantial part of why SEC-10 has gone unnoticed: the tier that was supposed to be granted deliberately could never be granted at all.
- **Fix:** `condition: (_data, _siblingData, { user }) => Boolean(user?.isAdmin)`. Fix this together with SEC-10 — tightening the collections without this leaves no way to give anyone the Editor role.

### [Low] SEC-21 — Live credentials sitting in the working tree
- **Where:** `.env` (21 populated values), `.env.sentry-build-plugin`
- **What:** Both are correctly gitignored (`.gitignore:29`, `:52`) so neither is in git — but both exist on disk and travel with any copy of the directory. Between them they hold the Payload secret, a Cloudinary API secret, an EasyPay API key, Google OAuth client secrets and refresh tokens for both Drive and Gmail, a Sentry auth token and a shadcn API key.
- **Why it matters:** Any time this directory is zipped, copied to a shared machine, or handed to a contractor or tool, all of that goes with it — which is what happened to produce this audit.
- **Fix:** Rotate `SENTRY_AUTH_TOKEN`, `PAYLOAD_SECRET`, `EASYPAY_API_KEY` and the Google refresh tokens as a precaution, and strip both files before sharing the directory.

---

## Performance & caching

`next.config.ts:74` enables `cacheComponents: true`, so **nothing is cached unless it is inside a `'use cache'` scope**. That makes the tag-to-hook cross-reference the load-bearing part of this section.

| Tag | Set in | Invalidated by |
| --- | --- | --- |
| `hawk-header` / `hawk-footer` | `lib/payload/queries/navbar.ts:10`, `:24` | ✅ `revalidateHeader.ts:7` / `revalidateFooter.ts:7` |
| `hawk-main-page` | `lib/payload/main-page.ts:8` | ✅ `revalidateMainPage.ts:7` |
| `news`, `hawk_projects`, `hawk_events`, `artworks`, `curators`, `partners`, `board-members`, `contributions`, `member_projects` | `queries/*.ts`, `helpers.ts:63` | ✅ `createRevalidateHooks` per collection |
| `pages`, `pages:<slug>` | `helpers.ts:63` | ❌ **never** — Pages only calls `revalidatePath` |
| `global:crowdfunding-settings`, `global:news-list`, `global:projects-list`, `global:events-list` | `helpers.ts:74` | ❌ **never** |
| `curators:<slug>` | ❌ commented out | n/a |

### [High] PERF-1 — Four editable globals are cached for an hour with no invalidation hook ✓
- **Where:** `lib/payload/queries/helpers.ts:72-75` (`cacheTag(\`global:${slug}\`)`, `cacheLife('hours')`); consumers `globals/crowdfundingSettings.ts:5`, `newsList.ts:18`, `projectsList.ts:25`, `eventsList.ts:11`. No `hooks` key on `CrowdfundingSettings/config.ts:14`, `NewsList/config.ts:8`, `EventsList/config.ts:13`, `ProjectsList/config.ts:13`.
- **What:** `findGlobalCached` tags every global read `global:<slug>`, but only Header, Footer and MainPage have `afterChange` hooks — and those three use their own tags, not `global:*`. The namespace is set by four globals and revalidated by zero. Verified by grepping every `revalidateTag` call in the tree: none of them takes a `global:` argument.
- **Impact:** An editor changing the crowdfunding funding total, phase or reward tiers, or the /news, /projects, /events page headers, waits **up to an hour** with no way to force it. During a live campaign the "raised so far" figure is the most time-sensitive number on the site.
- **Fix:** A `createRevalidateGlobalHook(slug)` mirroring `payload/utilities/revalidateCollection.ts`, added to all four configs.

### [High] PERF-2 — Page-builder blocks fetch CMS data from the browser, bypassing every cache
- **Where:** `payload/blocks/UpcomingHawkEventBlock/Component.tsx:27`, `LatestNewsBlock/Component.tsx:81`, `AgendaBlock/Component.tsx:173` (two parallel calls), `SponsorsBlock/Component.tsx:19`; transport `lib/payload/client-side/client.ts:15`
- **What:** Four blocks are `'use client'` and `useEffect`-fetch the Payload REST API after hydration, passing no `depth`/`select` — so REST returns depth-2 documents. `fetchAgendaProjects` pulls up to 20 full `hawk_projects` docs, each carrying the whole page-tab, partners and itinerary trees.
- **Impact:** A homepage with an Agenda + LatestNews + Upcoming block costs **four extra browser round trips and four uncached Mongo queries per visitor**, none touching the `'use cache'` layer the rest of the app is built on. All four render `null` until resolved, so the content is invisible to crawlers and arrives after hydration as a layout shift. Two side bugs found while reading: `lib/payload/client-side/queries/event.ts:61` sends the *event* query to `API_CLIENT_PATHS.projects`, and none of these calls pass `locale`, so EN pages get PT content.
- **Fix:** Convert each to a server component fed by a `'use cache'` + `cacheTag(...)` query in `lib/payload/queries/`, matching `getNewsQuery`. Only the presentational `*BlockView` children need `'use client'`.

### [High] PERF-3 — `import * as` from `react-icons/lu` lands in the root layout's client bundle
- **Where:** `lib/icon.tsx:1`, reached from `components/navbar/DesktopDropdown/utils.tsx:1` ← `DropdownComponent.tsx:4` ← `components/navbar/Navbar.tsx:14` (`'use client'`), rendered by `app/[lng]/(org)/layout.tsx:90`. Also `components/projects/list/HeroImpactStatsBlock.tsx:12`, `payload/fields/ImageIcon/components/Field.tsx:5`.
- **What:** `getIcon` looks up `LuIcons[prefixed]` by computed key. A namespace import plus a dynamic property read is un-tree-shakeable by construction, and `optimizePackageImports` cannot rewrite `import * as` either — `next.config.ts:76-81` sets no `optimizePackageImports`/`modularizeImports` at all.
- **Impact:** The entire Lucide set (~1,500 icon components) is pulled into the navbar chunk — every route, both locales — to render a handful of CMS-chosen icons.
- **Fix:** Replace the namespace import with an explicit allow-list map of the icons editors can pick (`payload/fields/ImageIcon/payload-lucide-icon.ts` already enumerates them), or `await import('react-icons/lu')` behind `next/dynamic`. Add `experimental.optimizePackageImports` for the other icon packs and `date-fns` while you are there.

### [High] PERF-4 — `RichText` statically imports all 45 blocks, ~20 of them client components
- **Where:** `payload/components/RichText/index.tsx:6-74` (45 static imports), registry at `:113`
- **What:** Every page rendered through `RichText` — the homepage, every CMS page, project and news bodies — imports the whole registry statically. Roughly 20 carry `'use client'`, including `DonationWidgetBlock`, `SimpleGallery` (pulls `embla-carousel-react`), `AgendaBlock`, `HeroSlideshowBlock`, `ImageComparisonSliderBlock`, `FAQBlock`, `NewsletterSignupBlock`, `CampaignCountdownBlock`.
- **Impact:** A page containing only a Hero and a MediaBlock still ships the client JS for the donation widget, the agenda calendar, embla and every carousel block. This is the largest single lever on JS-per-page for a block-based builder.
- **Fix:** Build the registry with `next/dynamic` for the client blocks and keep the pure-server ones static — the lazy-registry-function shape at `:113` already accommodates it.

### [High] PERF-5 — `autosave: { interval: 100 }` fires cache invalidation ~10×/second while an editor types
- **Where:** `payload/collections/News/index.ts:103-104`, `HawkProject/index.ts:215-216`, `Pages/index.ts:165-166` (Header uses 1000 at `Header/config.ts:165`)
- **What:** The interval is milliseconds. Each autosave runs `afterChange`, and neither `createRevalidateHooks` (`payload/utilities/revalidateCollection.ts:104-107`) nor `revalidatePage` (`Pages/hooks/revalidatePage.ts:22-43`) guards on `operation`, `_status` or `context.disableAutosave`. For Pages, autosaving an already-published doc takes the branch at `:35` and calls `revalidatePath` for **both locales**.
- **Impact:** One editor in one document produces up to 10 version writes per second to Mongo plus 10 `revalidateTag(tag, 'max')` calls (or 20 `revalidatePath`). `revalidateTag('news','max')` wipes `getNewsQuery` and every `news:<slug>` entry site-wide, and the co-tagged `hawk_projects` goes with it — so the cache never warms while anyone is editing.
- **Fix:** Raise the interval to Payload's default (2000, or ≥800) and early-return from the revalidate hooks when `doc._status !== 'published'` and the previous status was not published either. The notify hooks already do exactly this check (`News/hooks/notifyNewsChange.ts:25-26`).

### [High] PERF-6 — `/transparency` runs two unbounded `contributions` queries per request, at depth 2
- **Where:** `lib/payload/queries/contribution.ts:35-44`, `:46-58` → `lib/payload/endpoints/totalContributioValueQuery.ts:8-15`; both called at `app/[lng]/(org)/transparency/page.tsx:27-30`
- **What:** `getSumContributions` fetches **every confirmed contribution** at default depth 2, serialises the whole result into a `Response`, then `JSON.parse`s it back just to sum a numeric field. Both calls `await connection()`, so neither is cacheable and the route is fully dynamic. The comment at `transparency/page.tsx:22` ("Both queries are already cached") is wrong — only the unrelated `getChairsContributionsQuery` is.
- **Impact:** Two full-collection reads with relationship population on every page view, growing linearly with donations and unbounded by design (`limit: 0`).
- **Fix:** Add `depth: 0, select: { value: true }` and return a plain number instead of a `Response`; better, use a Mongo aggregation (`$match` + `$group`). Drop `connection()` from both and wrap them in `'use cache'` + `cacheTag(CONTRIBUTION_CACHE_TAG)` — the collection already ships the matching hooks.

### [High] PERF-7 — The homepage opts itself out of prerendering for no data reason
- **Where:** `app/[lng]/(org)/page.tsx:30` — `await connection()`
- **What:** The homepage's only data source is `getMainPageInformation` → `lib/payload/main-page.ts:7-9`, which is fully `'use cache'`, tagged and hook-invalidated. `connection()` marks the route dynamic anyway, and unlike the nine preview routes (which correctly use it) nothing here needs per-request state.
- **Impact:** The site's LCP route and both locale variants are server-rendered per request instead of served from the full-route cache, even though every byte of their content is already cached.
- **Fix:** Delete the line. If it was added to silence a prerender bailout from a client-fetching block, fixing PERF-2 removes the need.

### [Medium] PERF-9 — Pages' cached documents carry a tag nothing revalidates
- **Where:** tag set at `lib/payload/queries/helpers.ts:63` via `page.ts:12`; hooks at `payload/collections/Pages/hooks/revalidatePage.ts:18`, `:29` use `revalidatePath` only
- **What:** Pages is the one collection of ten that does not use `createRevalidateHooks`. Its `'use cache'` document entry is shared between `generateMetadata` and the page body and is keyed by tag, not path — so a `revalidatePath`-triggered re-render can be rebuilt from the same stale cached document.
- **Impact:** Editors may see a regenerated route still showing pre-edit content for up to the hour-long window. The audit comment in `revalidateCollection.ts:93-99` describes precisely this class of bug, which makes the omission look accidental.
- **Fix:** Add `createRevalidateHooks('pages')` alongside the existing `revalidatePath` hooks.

### [Medium] PERF-10 — `/projects`, `/events` and both archives read whole collections for a dropdown, uncached
- **Where:** `lib/payload/queries/projects.ts:50`, `:75` (`connection()`); `projects.ts:97-106` and `hawkEvent.ts:168-177` (`limit: 0`, uncached); called at `projects/page.tsx:63-64`, `events/page.tsx:60-61`, and both archives
- **What:** The `*YearsQuery` helpers fetch every project/event document purely to derive the distinct years for a filter `<select>`. They do pass `select`, so the projection is small — but the row count is unbounded and the call is uncached.
- **Impact:** Three uncached Mongo queries per request across eight routes (4 pages × 2 locales), scaling with total content forever.
- **Fix:** Wrap both `*YearsQuery` in `'use cache'` + the matching collection tag — the year list changes only when content does. Drop `connection()` from `getProjectsSplitByDate` and cache it the way `getHawkEventsSplitByDate` already is.

### [Medium] PERF-11 — No `generateStaticParams` on any content route
- **Where:** present only in `(org)/layout.tsx:20`, `(crowdfunding)/layout.tsx:56`, `(gaming)/layout.tsx:59`; absent from `[slug]`, `news/[slug]`, `events/[slug]`, `projects/[slug]`, `artwork/[slug]`, `curator/[slug]` — each file comments on the absence
- **Impact:** `params` is a dynamic API, so every detail page renders per request; the code has been restructured around this (non-async shells + `<Suspense>`) rather than prerendering. For a ~117-route content site whose slugs are already enumerated in `app/sitemap.ts:145-190`, no detail page gets a prerendered shell.
- **Fix:** Add `generateStaticParams` per route reusing the sitemap's `depth: 0, select: { slug: true }` finds; the cached data queries then fill them at build with no extra DB cost.

### [Medium] PERF-12 — `sitemap.ts` is uncached — six 1,000-document finds per crawler hit
- **Where:** `app/sitemap.ts:145-190`
- **What:** The queries are well written (`Promise.all`, `depth: 0`, `select`) but sit outside any `'use cache'`, and `lastModified: new Date()` at `:124` makes the output change on every request.
- **Impact:** Bots hit this often. The rolling `lastModified` also tells crawlers every static route changed just now, every time — see SEO-10.
- **Fix:** Wrap the body in `'use cache'` with `cacheLife('hours')` and the six collection tags; use a fixed build date for the static routes.

### [Medium] PERF-13 — `lib/flags` eagerly imports ~150 flag components into client bundles
- **Where:** `lib/flags/index.ts:1-151` → `lib/icon.tsx:3` → `components/projects/single/ProjectsSingleHero.tsx:5` and `SingleProjectReports.tsx:3`, both `'use client'`
- **What:** One import statement covering ~150 countries, with the map built at module scope, so the bundler must keep every flag component even though a project page renders a handful.
- **Fix:** Per-country dynamic import from `country-flag-icons/react/3x2/<CC>`, or split `FlagIcons` into its own module so `getIcon`'s other consumers do not pull it transitively.

### [Medium] PERF-14 — Instagram feed: a DB read per request plus a post-hydration waterfall
- **Where:** `app/api/instagram/route.ts:133-134` (uncached `payload.findGlobal({ slug: 'settings' })`, no `depth`/`select`), consumed by `components/socials/InstagramFeed/InstagramGrid.tsx:74-91`; embed at `InstagramEmbedWidget.tsx:104-111`
- **What:** The Graph API call itself *is* correctly cached (5 min, tagged), but the settings lookup that supplies the token runs before it on every request. On the client the grid renders a skeleton and fetches after hydration, and the embed variant injects `https://www.instagram.com/embed.js` with a hand-rolled `document.createElement('script')` rather than `next/script`.
- **Fix:** Move the settings read inside a `'use cache'` wrapper tagged `global:settings`; render the grid server-side from the cached fetch; load `embed.js` with `next/script strategy="lazyOnload"`.

### [Medium] PERF-15 — Time-dependent event buckets are frozen into an hour-long cache
- **Where:** `lib/payload/queries/hawkEvent.ts:114-116`, with `where` clauses built from `customDateRangeQuery()` at `:133`, `:166`
- **What:** "Today / upcoming" boundaries are computed *inside* the cached scope, so they are baked into the entry; the cache key is only `(locale, type, year)`.
- **Impact:** Around midnight, and for up to an hour after an event starts, `/events` shows it in the wrong bucket.
- **Fix:** Pass the day boundary in as an argument so it becomes part of the cache key, or use `cacheLife({ revalidate: 900 })` for these two buckets.

### [Medium] PERF-16 — Over-fetch depth on server queries
- **Where:** `lib/payload/queries/projects.ts:97` (`depth: 3` on every project detail page), `contribution.ts:38` and `totalContributioValueQuery.ts:11` (default depth 2 on contributions), `navbar.ts:126`, `:140` (`depth: 2`, mitigated by `populate` on the footer only)
- **Impact:** `depth: 3` resolves relationships three levels deep on the largest document type in the system, and the same payload is then serialised into the RSC stream.
- **Fix:** Drop to `depth: 2` and add `populate`/`select` for the fields actually rendered — `navbar.ts:142-149` shows the pattern.

### [Medium] PERF-17 — Every link on the site is a client component in order to read the locale
- **Where:** `components/utils/HawkLink/InternalHawkLink.tsx:4` and `ExternalHawkLink.tsx:1` both call `useLanguageCookie()`, forcing `HawkLink/index.tsx:1` `'use client'`
- **What:** `lng` is a route param available synchronously on the server and is already passed explicitly to most components; reading it from React context is what makes these links client-only.
- **Impact:** The entire footer and nav link tree hydrates on every page for a value the server already knows.
- **Fix:** Add an optional `lng` prop that short-circuits the context read; keep the context as the fallback for the genuinely-client navbar.

### [Medium] PERF-18 — Client-side i18n re-downloads strings already server-rendered
- **Where:** `i18n/client.ts:28-31`
- **What:** `resourcesToBackend((language, namespace) => import('./locales/${language}/${namespace}.json'))` generates a context module over all 2 × 19 locale files (~76 KB per locale). Every `useTranslation` in a client component fetches its namespace chunk after hydration, re-downloading text already in the HTML.
- **Fix:** For components that already receive `lng` from the server, pass the handful of needed strings as props — the pattern `app/[lng]/(org)/agenda/page.tsx:31-61` already uses.

### [Medium] PERF-19 — Sentry browser sampling is high for a content site
- **Where:** `instrumentation-client.ts:22` (`inheritOrSampleWith(0.5)`), `:28`, `:32` (`replaysOnErrorSampleRate: 0.1`, `replaysSessionSampleRate: 0.1`)
- **Impact:** Half of all visitors upload performance spans; 1 in 10 run the replay recorder for the whole session (~50 KB lazy JS plus continuous DOM capture) on a site with no interactive product surface to debug. Replay is at least lazily loaded, which is the right call.
- **Fix:** `inheritOrSampleWith(0.05)` and `replaysSessionSampleRate: 0`, keeping `replaysOnErrorSampleRate`.

### [Medium] PERF-20 — PM2 runs a single fork-mode process
- **Where:** `ecosystem.config.cjs:109-110` (`instances: 1`, `exec_mode: 'fork'`)
- **Impact:** One Node process serves all traffic, including `next/image` optimisation via sharp — the most CPU-hungry work on an image-heavy site. A single slow render blocks the event loop for everyone. nginx already pools keepalive to `127.0.0.1:3000`, so scaling is a config change.
- **Fix:** `exec_mode: 'cluster'` with 2+ instances, raising `cacheMaxMemorySize` per instance. **Caveat:** each instance gets its own in-memory cache, so `revalidateTag` from a Payload hook reaches only the worker that handled the save — pair this with a shared cache handler or accept TTL-only propagation on the others.

### [Medium] PERF-21 — nginx proxies all HTML to Node with no cache, and references undeclared zones
- **Where:** `nginx.conf:166-179` (`location /` has `limit_req` + `proxy_pass`, no `proxy_cache`); `:4-9` claims `limit_req_zone` and `proxy_cache_path` are declared "immediately below", but only `upstream app` (`:13`) is — while `assets_cache` is referenced at `:73`, `:98`, `:117`, `:151` and `mylimit` at `:167`
- **Impact:** Every HTML and RSC request reaches the single Node process. There is also no `gzip`/`gzip_types` block here, so text compression depends on the parent `http{}` config. The undeclared zones mean this file cannot start standalone — either they live in the host config (and the comment is misleading) or the rate limit the API handlers assume as a backstop does not exist.
- **Fix:** Add a short micro-cache for document responses (`proxy_cache_valid 200 10s; proxy_cache_lock on;` with a bypass for `/admin` and `/preview`), and either declare both zones here or correct the comment.

### [Low] PERF-22 — `getMainPageInfo` caches preview renders
- **Where:** `lib/payload/main-page.ts:7-9` — `'use cache'` sits above the `opts?.preview` branch at `:14`, unlike `helpers.ts:92-94` which deliberately bypasses the cache for drafts
- **Impact:** The admin live preview lags behind autosaves by up to an hour.
- **Fix:** Mirror `findPublishedBySlug` — call the uncached `payload.findGlobal` directly when `preview` is true.

### [Low] PERF-25 — `react-dom/server` in the Leaflet client chunk
- **Where:** `components/ui/map.tsx:69`
- **What:** `renderToString` is used to stringify marker icons. The module is only reachable through a `dynamic(..., { ssr: false })` wrapper so it stays out of the main bundle, but it bloats the map chunk, which also statically imports `leaflet` and `react-leaflet` alongside its own per-component `dynamic()` calls.
- **Fix:** Build marker HTML as a template string, and drop the redundant static `react-leaflet` import.

---

## Code quality, CI & deploy

### [High] QUA-32 — `tsc --noEmit` now fails: the sitemap fix added routes with no metadata entries ✓ **[regression introduced 2026-09-15]**
- **Where:** `utils/metadata.ts:54`, caused by `utils/paths.ts:28-29` and `:102-103`
- **What:** Executed, not inferred. One error:
  ```
  utils/metadata.ts(54,28): error TS7053: Element implicitly has an 'any' type because expression of type
  '"partners" | "news" | "gaming" | … | "terms"' can't be used to index type '{ home: …; donate: …; … }'.
  Property 'gaming' does not exist on type …
  ```
  Closing SEO-3 added `crowdfunding: '/crowdfunding'` and `gaming: '/gaming'` to `SITE_GET_URLS`, which widens the union that keys `getMetadataPageInfo`. Neither key exists in `i18n/locales/en/metadata.json` or its `pt` counterpart — I checked: the file has 21 keys and neither of these is among them.
- **Impact:** **The typecheck gate is red**, so together with QUA-30 two of the three `check` steps now fail. `pnpm build` will fail for the same reason, which means this is not merely a CI annoyance — the site cannot be deployed in this state. Beyond the compile error, the underlying gap is real: the two new sitemap routes have no title or description defined, so even once it compiles they would fall back to whatever the index returns for a missing key.
- **Fix:** Add `crowdfunding` and `gaming` entries (title + description) to `i18n/locales/en/metadata.json` and `i18n/locales/pt/metadata.json`. That closes the type error and gives the two pages the metadata they now need, since they are being submitted for crawling.

### [High] QUA-30 — `eslint .` fails with 5 errors, so the CI lint gate is red ✓ **[re-verified 2026-09-15 — unchanged]**
- **Where:** `components/ui/map.tsx:305:7`, `:752:14`, `:753:23`, `:1057:5`, `:1188:7` (errors); `:657`, `:722`, `:730`, `:1002` and `components/members-corner/MembersShowcase.tsx:15:21` (warnings)
- **What:** Re-run on the current tree: `eslint .` exits 1 with **10 problems (5 errors, 5 warnings)**. The five errors are identical to the first pass and are all in `components/ui/map.tsx`:
  - `:752:14`, `:753:23` — **`Cannot access refs during render`** (`react-hooks/refs`)
  - `:305:7`, `:1188:7` — **`Calling setState synchronously within an effect can trigger cascading renders`** (`react-hooks/set-state-in-effect`)
  - `:1057:5` — **`This value cannot be modified`** (`react-hooks/immutability`), on `L.drawLocal.edit.handlers.edit.tooltip = { … }` — mutating a value returned from a hook
  `components/ui/map.tsx` was touched since the first pass (18 lines changed) but none of the five errors were addressed. The warning count went from 3 to 5: two new `exhaustive-deps` warnings at `:722`/`:730`, plus the dead `iso` parameter from the I18N-4 regression.
- **Impact:** `pnpm lint` is the first step of the `check` job, so no PR passes CI and no push to `main` deploys. The two `refs during render` errors and the two `setState in effect` errors are correctness rules, not style — accessing a ref during render is undefined behaviour under concurrent rendering, and the `L.drawLocal` mutation writes to a shared global from inside a component, so two maps on one page fight over the tooltip text.
- **Fix:** Move the ref reads into an effect or an event handler; move the two `setState` calls behind a condition or into the external-system callback they belong to; set `L.drawLocal` once at module scope or in a run-once effect.

### [High] QUA-1 — The deploy stops and *deletes* PM2 and wipes `.next` before installing and building ✓
- **Where:** `.github/workflows/deploy.yml:158-176`, with `set -e` at `:97` and `git reset --hard origin/main` already done at `:110`
- **What:** The order is `pm2 stop` → `pm2 delete` → `rm -rf .next` → `pnpm install --frozen-lockfile` → `pnpm build` → `pnpm migrate` → `pm2 start`.
- **Impact:** The site is offline from line 158 until line 176. Any failure in install, build (which needs a 4 GB heap, so OOM is plausible) or migrate trips `set -e`, the script exits, PM2 never restarts — and the app has been **deleted** from the process list, so even a reboot with `pm2 resurrect` gets nothing, because `pm2 save` is at line 178, after the failure point. The previous working tree is gone too. Recovery is manual SSH. There is no rollback path anywhere in the workflow.
- **Fix:** Build first, restart last. Build while the old process keeps serving, then `pm2 reload hawkstars` (zero-downtime) only after build and migrate both succeed. Keep the previous SHA in a variable and reset + rebuild + reload on failure.

### [High] QUA-2 — The `check` job never runs `pnpm build` ✓
- **Where:** `.github/workflows/deploy.yml:35-42`
- **What:** The only gating job runs `pnpm lint`, `pnpm tsc`, `pnpm test`. No build step. `next build` is where `generateStaticParams`, the 16 `'use cache'` call sites, `cacheComponents: true` and `staticPageGenerationTimeout: 120` actually get exercised — none of which `tsc --noEmit` can catch.
- **Impact:** Combined with QUA-1, a prerender error or static-generation timeout is discovered only on the VPS, after PM2 has been deleted and `.next` removed. The Husky `pre-push` hook does run `pnpm build`, but it is skipped when `CI=true` and bypassed by `git push --no-verify` or a merge in the GitHub web UI.
- **Fix:** Add a `pnpm build` step to `check` — it already has Node, pnpm and deps installed.

### [High] QUA-3 — No `concurrency` group on the deploy workflow ✓
- **Where:** `.github/workflows/deploy.yml:1-12`; compare `db-backup.yml:33-35`, which *does* set one
- **Impact:** Two merges a minute apart give two concurrent SSH sessions in `~/app` doing `git reset --hard`, `rm -rf .next`, `pnpm install`, `pnpm build` and `pm2 start` against the same files — outcomes range from a corrupted `.next` to two PM2 apps fighting over port 3000.
- **Fix:** `concurrency: { group: deploy-production, cancel-in-progress: false }` at workflow level.

### [High] QUA-4 — The deploy reports success without verifying the app came up — and the health endpoint built for exactly this is never called ✓
- **Where:** `app/api/health/route.ts:5-13` vs `.github/workflows/deploy.yml:176-194`
- **What:** The route's own docstring: *"Used by the deploy workflow to confirm a freshly started PM2 process is actually serving traffic (and can reach the DB) before the deploy is considered successful — previously there was no such check, so a build that compiled but crash-looped on boot was reported as a successful deploy."* The workflow contains no `curl` step: `pm2 start` → `pm2 save` → `deployment-status: success`, and `notify` then posts "✅ Deployed to production".
- **Impact:** `ecosystem.config.cjs:19-21` sets `max_restarts: 10` and `min_uptime: '20s'`, so a process that crash-loops on boot is retried ten times and then goes dormant. `pm2 start` returns 0 immediately. The site 502s while GitHub and Discord both report success.
- **Fix:** After `pm2 start`, `curl -fsS --retry 10 --retry-delay 3 http://127.0.0.1:3000/api/health` and fail (and roll back) on non-200.

### [High] QUA-5 — Rich-text block converters are cached against the *first* locale rendered by the process ✓
- **Where:** `payload/components/RichText/index.tsx:170-180`
- **What:** `let blockConvertersCache … = null;` then `blockConvertersCache ??= Object.fromEntries(… blockConverter(Component, lng) …)`. `blockConverter` (`:166`) closes over `lng` and renders `<Component {...node.fields} lng={lng} />`. The cache key is nothing — `??=` fills it once per module instance and never again.
- **Impact:** `RichText` correctly does `useMemo(() => jsxConverters(lng), [lng])` at `:213`, but that memo calls `getBlockConverters(lng)`, which ignores its argument after the first call. In the long-lived PM2 fork, whichever locale renders a rich-text block first wins until restart: every block on every `/en/…` page then receives `lng='pt'` and builds its internal links with `transformUrl('pt', …)`. Silent, and only reproducible after a restart in the "wrong" order — so it will not show up in dev.
- **Fix:** Key by locale: `const cache = new Map<Language, …>()`, returning the per-locale entry. The stability requirement the comment describes is preserved as long as each locale's converters are built once.

### [Medium] QUA-6 — Two workflow secrets are exported in `env:` but omitted from the ssh-action `envs:` whitelist, so they arrive empty ✓
- **Where:** `.github/workflows/deploy.yml:87-88` (declared) vs `:95` (forwarded) vs `:102` and `:151` (consumed)
- **What:** `envs:` ends at `EASYPAY_WEBHOOK_SECRET`. `PERSONAL_GITHUB_TOKEN` and `NEXT_PUBLIC_CARTO_BASEMAP_KEY` are not in the list, and `appleboy/ssh-action`'s `envs` is a whitelist. Verified by diffing the `.env` heredoc keys against the list; `DATABASE_URI` and `NODE_ENV` are also absent but are hardcoded literals, so only these two are real.
- **Impact:** Line 102 becomes `git config --global url."https://:@github.com".insteadOf …` — a credential rewrite with an empty token. Line 151 writes `NEXT_PUBLIC_CARTO_BASEMAP_KEY=` into production's `.env` on every deploy, so the Carto basemap has been running unauthenticated in production.
- **Fix:** Append `,PERSONAL_GITHUB_TOKEN,NEXT_PUBLIC_CARTO_BASEMAP_KEY` to line 95.

### [Medium] QUA-7 — Nothing fails on lint *warnings*, and `prettier --check` is never run in CI
- **Where:** `eslint.config.mjs:34-51`, `.github/workflows/deploy.yml:35-36`
- **What:** Both custom rules are `'warn'` (`no-unused-vars`, `max-len` at 140). CI runs bare `eslint .`, which exits non-zero only on *errors* — the three `exhaustive-deps` warnings in QUA-30 would pass silently on their own. `pnpm format` (`prettier --check .`) is in no workflow, though it currently passes clean.
- **Impact:** The standards documented in `.claude/ops.md` ("Max line length 140 — ESLint enforced") are advisory only, and formatting drift would reach `main` unnoticed since the pre-commit hook does not re-stage (QUA-8).
- **Fix:** `pnpm lint -- --max-warnings 0` plus a `pnpm format` step.

### [Medium] QUA-31 — Five dangling `AUDIT.md` references, three of them shown to content editors in the CMS ✓ **[reframed 2026-09-15]**
- **Where:** `next.config.ts:27`, `components/utils/Input/Input.tsx:30`, `payload/blocks/Hero/config.ts:36-37`, `payload/blocks/HeroSlideshowBlock/config.ts:24-25`, `payload/blocks/HeroWithBackgroundImage/config.ts:33-34`
- **What:** `AUDIT.md` has now been deliberately removed and replaced by `AUDIT-2026-09-14.md` (both committed), so the earlier ambiguity about whether the deletion was intentional is resolved. The references to it were not updated. Three of the five are Payload field `description` strings rendered as help text in the admin UI, in both PT and EN — e.g. `payload/blocks/Hero/config.ts:36`: *"… a page must have exactly one H1 (AUDIT.md A11Y-M3)."*
- **Why it matters:** A content editor is told to consult a file they have no way to open, and the finding IDs cited (`SEC-M3`, `A11Y-M3`) came from the old scheme and do not exist in the current document either, so even a developer who finds the new file cannot resolve them. The CSP rationale at `next.config.ts:8-27` points at the same dead end.
- **Fix:** Inline the one-sentence rationale into the three CMS-visible descriptions so editor-facing help never depends on a repo file, and repoint the two code comments at `AUDIT-2026-09-14.md` with current IDs (SEC-12 for the CSP note, A11Y-10 for the `Input.tsx` one) — or drop the citation and keep the explanation.

### [Medium] QUA-8 — `pre-commit` runs `format:fix` but never re-stages
- **Where:** `.husky/pre-commit` (a single line: `pnpm format:fix`)
- **What:** `format:fix` is `prettier --write .`. Git has already snapshotted the index when the hook runs, so Prettier's rewrites land unstaged.
- **Impact:** The commit contains the unformatted version, and the developer is left with a dirty tree that rides along on the next commit. It also formats the entire repo, not just staged files.
- **Fix:** `lint-staged`, or at minimum `pnpm format:fix && git add -u`.

### [Medium] QUA-9 — `LIBRETRANSLATE_URL` is never written to the production `.env`
- **Where:** `payload/endpoints/translate.ts:24`, `.github/workflows/deploy.yml:129-152`, `payload.config.ts:134-140`
- **What:** `const baseUrl = (process.env.LIBRETRANSLATE_URL || 'http://localhost:5000')…`. The deploy heredoc writes 20 variables; this is not one of them. Meanwhile the feature is fully wired into the admin UI via `afterNavLinks` and a `/translate` view.
- **Impact:** Editors see a Translate link and a Translate view that fail on every use in production, each failure landing in Sentry. `GEOCODING_CONTACT_EMAIL` has the same gap but a sane fallback.
- **Fix:** Add it to the heredoc and the `envs:` list, or hide the nav link when the variable is unset.

### [Medium] QUA-10 — `pnpm tsc` is not a script, so CI and the pre-push hook bypass the documented typecheck and its heap flag ✓
- **Where:** `.github/workflows/deploy.yml:39`, `.husky/pre-push:2`, vs `package.json:10`
- **What:** `package.json` defines `"typecheck": "NODE_OPTIONS=--max-old-space-size=4096 tsc --noEmit"`. There is no `"tsc"` script. Both call sites run `pnpm tsc`.
- **Impact:** Whatever that resolves to, it is not the documented script, and it runs without the heap bump that a 177 KB `payload-types.ts` plus 673 TS/TSX files was sized for.
- **Fix:** Change both to `pnpm typecheck`.

### [Medium] QUA-13 — Unhandled promise rejections in client blocks; the added `.catch(null)` does nothing ✓ **[partially addressed 2026-09-15]**
- **Where:** `payload/blocks/LatestNewsBlock/Component.tsx:21`, `:23`; `payload/blocks/SponsorsBlock/Component.tsx:20`; `components/ui/map.tsx:1162`, `:1165`
- **What:** `LatestNewsBlock` now reads `fetchLatestHawkEvent(eventType).then(setItem).catch(null);`. **`.catch(null)` is a no-op.** `Promise.prototype.catch(onRejected)` ignores a non-callable argument and passes the rejection straight through, so this is exactly equivalent to the code before the change — the rejection is still unhandled, and nothing is reported. `SponsorsBlock/Component.tsx:20` and both `map.tsx` sites were not touched.
- **Why it matters:** The behaviour is unchanged from the original finding: a network failure or 500 makes the block render `null` and vanish from the page, with no user-visible error and no Sentry event, leaving an editor with an empty section and nothing to debug. `map.tsx` remains the worst case — a stale-chunk dynamic-import failure after a deploy is explicitly listed in `sentry.edge.config.ts:48-50` as an ignored error, so the map silently never renders. The change is worth calling out because it now *reads* as handled, which makes it less likely anyone looks again.
- **Fix:** `.catch((e) => { Sentry.captureException(e); setError(true); })` at all five sites, and render a distinct fallback when the fetch *failed* as opposed to returning no data.

### [Medium] QUA-14 — `(crowdfunding)` and `(gaming)` route groups have no `error.tsx`
- **Where:** both groups contain only `layout.tsx`, `loading.tsx` and pages; compare `app/[lng]/(org)/error.tsx`
- **What:** `(org)/error.tsx:21-28` documents exactly why this matters: *"Without this file, a thrown error inside any page here escalates all the way up to `app/global-error.tsx`, which replaces the entire document — losing the Navbar/Footer/AppProvider chrome…"*. That reasoning was never applied to the other two groups.
- **Impact:** A render error on `/pt/crowdfunding` or `/pt/gaming` yields an unstyled, untranslated, un-navigable page — on the two sub-sites with the most client-heavy components.
- **Fix:** Add an `error.tsx` to each, reusing the `(org)` one.

### [Medium] QUA-16 — Actions with full secret access are pinned to mutable tags
- **Where:** `.github/workflows/deploy.yml:19`, `:22`, `:27`, `:52`, `:57`, `:65`, `:182`, `:208`, `:213`
- **What:** `appleboy/ssh-action@v1` receives the SSH key, passphrase, host and 19 application secrets. `actions/checkout@v7` at `:52` receives `PERSONAL_GITHUB_TOKEN`. All floating tags. The Chromatic action at `:221` is the one that *is* SHA-pinned.
- **Impact:** A compromised or retagged upstream action gets the production SSH key and every payment and Google credential, with no change to this repo.
- **Fix:** Pin all third-party actions to a full commit SHA, the way `chromaui/action` already is.

### [Medium] QUA-17 — Test coverage is concentrated on payments; rate limiting, access control and URL logic are untested
- **Where:** the only test files are `app/api/donate/route.test.ts` (352 lines), `easypay/route.test.ts` (350), `subscription/route.test.ts` (328)
- **What:** ~70 tests, all against the three EasyPay routes — and genuinely good ones (webhook secret rejection including the query-string variant, fail-closed when unset, 200-on-internal-error, validation, env-var absence). What has **zero** tests: `utils/rateLimit.ts` (whose `getClientIp` takes the *last* `X-Forwarded-For` entry specifically because taking the first let anyone defeat the limiter — that security fix is untested, and the module exports `resetRateLimit()` purely as a test helper); all four functions in `payload/access/*`; `utils/paths.ts` (`transformUrl`, `isHttpUrl` — the latter a validation dependency of `/api/member-projects:9`); and both remaining unauthenticated public write endpoints, `/api/newsletter` and `/api/member-projects`.
- **Fix:** A pure-unit suite for `getClientIp` (XFF spoofing cases), the four access functions and `transformUrl`. No mocking needed; under an hour of work.

### [Low] QUA-18 — An untracked `package-lock.json` sits beside the tracked `pnpm-lock.yaml` ✓
- **Where:** repository root; `.gitignore:52` ignores `package-lock.json`
- **What:** `pnpm-lock.yaml` is committed (verified with `git ls-files`). `package-lock.json` exists on disk, dated 2026-05-09, untracked and ignored.
- **Impact:** Because it is *ignored* rather than forbidden, a developer who runs `npm install` by mistake produces a divergent, invisible dependency tree locally with nothing to flag it, while CI and the deploy both run `pnpm install --frozen-lockfile`.
- **Fix:** Delete the stale file and add a `preinstall` guard (`npx only-allow pnpm`) so `npm install` fails fast.

### [Low] QUA-20 — `detectLanguage` is duplicated verbatim in two adjacent files
- **Where:** `app/[lng]/(org)/error.tsx:13-19` and `not-found.tsx:11-17` — identical seven-line bodies
- **Fix:** Move to `i18n/settings.ts` or `utils/paths.ts`.

### [Low] QUA-21 — Four of 45 blocks have no Storybook story
- **Where:** `HeroSlideshowBlock/`, `HeroWithBackgroundImage/`, `InstagramBlock/`, `OfferCatalogBlock/`
- **What:** `.claude/rules/frontend/payload.md` states every block folder has "exactly three files"; the other 41 comply.
- **Impact:** These four are excluded from Chromatic visual regression and from the a11y checks in `.storybook/preview.ts:31` — and two of them are hero components, the largest above-the-fold surfaces.
- **Fix:** Add stories. Naming is already inconsistent across the tree (`AccordionBlock.stories.tsx` vs `Component.stories.tsx`); pick one.

### [Low] QUA-22 — Client-side Sentry is enabled in every environment except `development`
- **Where:** `instrumentation-client.ts:35` (`enabled: process.env.NODE_ENV != 'development'`) vs `sentry.server.config.ts:13` and `sentry.edge.config.ts:74`, both `=== 'production'`
- **Impact:** During `pnpm test` and Storybook builds (`NODE_ENV=test`) the browser SDK is live and ships events into the production Sentry project.
- **Fix:** Make it `=== 'production'` to match.

### [Low] QUA-23 — Two empty catch blocks swallow share/clipboard failures with no user feedback
- **Where:** `components/Crowdfunding/ShareButton.tsx:24`, `:30` — `} catch {}` twice
- **Impact:** If `navigator.clipboard.writeText` is blocked (non-secure context, permission denied), the button does nothing at all — `setCopied(true)` never runs, so there is not even a visual response. The first catch (a cancelled `navigator.share`) is legitimately silent.
- **Fix:** Set an error state on the second and show a selectable URL fallback; add a one-line comment on the first.

### [Low] QUA-25 — `.env.example` drift
- **Where:** `.env.example:9`
- **What:** `NEXT_PUBLIC_BASE_URL` appears in no `.ts`/`.tsx` file; the three real consumers all read `NEXT_PUBLIC_APP_URL`. Meanwhile `NEXT_PUBLIC_CARTO_BASEMAP_KEY` is in the live `.env` and the workflow but absent from the example, so a new developer never sets it.
- **Fix:** Delete line 9; add the Carto key.

### [Low] QUA-26 — Two overlapping Sentry capture helpers
- **Where:** `lib/sentry/logs.ts` (used by four API routes) and `lib/sentry/cli-client.ts` (used by exactly one file)
- **Impact:** Three ways to report to Sentry, counting bare `Sentry.captureException` in ~8 files. `cli-client.ts` has the more robust behaviour — falls back to `@sentry/node` in CLI contexts, wraps in try/catch so telemetry never masks the original error — but only one caller.
- **Fix:** Make `cli-client.ts` the single entry point and have `logs.ts` delegate.

### [Low] QUA-28 — Two live `TODO` comments
- **Where:** `components/utils/DatePicker/DatePicker.tsx:3` (`// TODO: check if we need this component`) and `utils/paths.ts:4`
- **What:** These are the only TODO/FIXME/HACK markers in the entire source tree.
- **Fix:** Resolve or track. `DatePicker` *is* imported (by its story and by form components), so the first can simply be deleted.

### [Low] QUA-29 — `.claude/ops.md` describes the pre-push hook incompletely
- **Where:** `.claude/ops.md` — "runs `pnpm lint` + `pnpm tsc` + `pnpm build`"
- **What:** `.husky/pre-push:3` also runs `pnpm test`, the slowest step by far.
- **Fix:** Add it to the doc.

---

## SEO

### [High] SEO-1 — Multi-segment unknown URLs return HTTP 200 (soft 404), including every bad-locale redirect ✓
- **Where:** `app/[lng]/(org)/[...notfound]/page.tsx:28-57`
- **What:** The catch-all renders a page body and never calls `notFound()`. Compare `[slug]/page.tsx:42` (`if (!pageInformation) notFound();`), which does produce a real 404. Because `[slug]` wins for single-segment paths, only **multi-segment** misses hit this 200 page.
- **Impact:** This is also the designed landing spot for unsupported locales: `utils/middlewares/withHandleInternalization.ts:52-59` 301-redirects `/fr/news` → `/pt/fr/news`, and its comment at `:51` claims this "resolves to the catch-all 404". It does not. Every foreign `Accept-Language` bot, every stale deep link and every mistyped path yields an indexable 200. `robots: 'noindex, nofollow'` stops indexing but not soft-404 reports in Search Console, wasted crawl budget, or the hiding of real broken links from analytics.
- **Fix:** Delete the file and let `not-found.tsx` handle it (Next returns 404 for unmatched routes automatically), or keep it and make the body call `notFound()`.

### [High] SEO-2 — Draft `pages` are published to the sitemap ✓
- **Where:** `app/sitemap.ts:39-45` vs `:60-67` and `:69-76`
- **What:** The file defines `const publishedWhere = { _status: { equals: 'published' as const } };` at `:35` and its comment at `:31-34` states *"Only `pages`, `news` and `hawk_projects` enable `versions.drafts`"* — yet the `pages` query omits it, while `hawk_projects` and `news` both pass it. Verified: `payload/collections/Pages/index.ts:163-171` does enable `versions.drafts` with autosave and `schedulePublish`.
- **Impact:** Every never-published and scheduled-but-not-yet-live page is submitted to Google in both locales. Those URLs 404 or leak unreleased content. With autosave at 100 ms, editors create draft pages constantly.
- **Fix:** Add `where: publishedWhere` to the `pages` find.

### [Medium] SEO-4 — Every Event JSON-LD is emitted without the required `location`
- **Where:** `app/[lng]/(org)/events/[slug]/page.tsx:55` (`location={undefined}`) and `components/seo/JsonLd.tsx:152-162`
- **What:** The call site hardcodes `undefined`, and `EventJsonLd` builds `location: location ? {…} : undefined`, which `JSON.stringify` drops. `payload/collections/HawkEvent/HawkProjectFields.ts` has no location/venue field at all, so nothing could populate it.
- **Impact:** `location` is required for Google's Event rich result, so every event page emits structured data that fails validation and no event ever qualifies. `eventStatus`, `eventAttendanceMode` and `offers` are also absent.
- **Fix:** Add a location group (venue + `PostalAddress`) to the collection and thread it through; until then, do not emit `Event` JSON-LD at all rather than emitting an invalid graph.

### [Medium] SEO-5 — `WebSite` SearchAction points at a search endpoint that does not exist, with an unresolvable variable
- **Where:** `components/seo/JsonLd.tsx:47-54`
- **What:** `` urlTemplate: `${BASE_URL}/{lng}/projects?q={search_term_string}` `` with `'query-input': 'required name=search_term_string'`. Two defects: `{lng}` is a template variable with no matching `query-input` declaration, which invalidates the `EntryPoint`; and a grep for a `q` search param across the whole app returns only this line — `projects/page.tsx` reads `type`/`year`/pagination only.
- **Impact:** A Sitelinks Searchbox declared against a URL that returns an unfiltered project list, shipped from the root layout so it is on every page.
- **Fix:** Remove the `potentialAction` block until a real site search exists.

### [Medium] SEO-6 — Paginated and filtered list URLs all canonicalize to page 1
- **Where:** `app/[lng]/(org)/news/page.tsx:25-29` → `utils/metadata.ts:88-90`
- **What:** `generateMetadata` ignores `searchParams` entirely, and `createMetadataObject` builds the canonical from the static path. Meanwhile `components/utils/Pagination/index.tsx:40-45` generates `?page=N&limit=N` links and `ListFilters/index.tsx:45-61` generates `?type=…&year=…` links, all crawlable.
- **Impact:** `/pt/news?page=4` declares `/pt/news` as canonical, so Google drops pages 2+ and articles that scroll off page 1 become orphaned. The filter combinations multiply the duplicate URL surface with no `noindex`.
- **Fix:** Have list-page `generateMetadata` await `searchParams` and emit a self-referencing canonical including `page`; add `robots: { index: false }` for filter-only variants.

### [Medium] SEO-7 — Structured data claims US 501(c)(3) status for a Portuguese NGO
- **Where:** `components/seo/JsonLd.tsx:37` — `nonprofitStatus: 'Nonprofit501c3'`, alongside `addressCountry: 'PT'` (`:30`) and `alternateName: 'Associação HawkStars'` (`:19`)
- **Impact:** A factually false machine-readable claim about legal and charitable status, published site-wide, on a site that solicits donations. A trust and potentially a compliance issue, not a schema nit.
- **Fix:** Remove the property, or use a `NonprofitType` matching the actual Portuguese registration.

### [Medium] SEO-8 — Multiple `<h1>` per page, guaranteed on any slideshow with more than one slide
- **Where:** `payload/blocks/HeroSlideshowBlock/Component.tsx:28` and `:126`
- **What:** `const HeadingTag = data.headingLevel === 'h2' ? 'h2' : 'h1';` is computed once, then rendered **inside the per-slide `.map()`**. Non-current slides are hidden only with `opacity-0` (`:94`), so all of them are in the DOM.
- **Impact:** A four-slide hero emits four `<h1>`. Separately, `Hero/Component.tsx:19` and `HeroWithBackgroundImage/Component.tsx:20` each default to `h1`, so stacking two hero blocks on one page-builder page adds two more. Search engines and screen readers see a page with no single unambiguous topic.
- **Fix:** Render `h1` only for `index === 0` (or hoist one visually-hidden `<h1>` and make slide titles `h2`), and mark inactive slides `aria-hidden` + `inert` — which also fixes A11Y-6.

### [Low] SEO-9 — `robots.ts` hardcodes the production sitemap URL
- **Where:** `app/robots.ts:22` (`sitemap: 'https://hawkstars.org/sitemap.xml'`) vs `lib/constants.ts:9` (`BASE_URL`, env-driven with a fallback)
- **Impact:** A staging or preview deployment serves a robots.txt pointing crawlers at the production sitemap.
- **Fix:** `` sitemap: `${BASE_URL}/sitemap.xml` ``.

### [Low] SEO-10 — `lastModified` for all 19 static routes is always "now"
- **Where:** `app/sitemap.ts:17`
- **What:** `lastModified: new Date()` inside the static-route loop, while CMS entries correctly use `updatedAt`.
- **Impact:** Every crawl claims `/team`, `/history`, `/store/terms` changed today. Google learns the signal is noise and discounts it for the whole sitemap, including the accurate entries.
- **Fix:** Drop it for static routes, or derive it from a build timestamp.

### [Low] SEO-11 — `generateMetadata` returns `{}` for missing documents, leaving 404s with no `<title>`
- **Where:** `[slug]/page.tsx:19`, `news/[slug]/page.tsx:25`, `events/[slug]/page.tsx:18`
- **What:** `if (!article) return {};` — and `(org)/layout.tsx` exports no `metadata` or `generateMetadata` to inherit from.
- **Fix:** Return `{ title: '404 …', robots: 'noindex, nofollow' }`.

---

## Internationalization

### [High] I18N-1 — Single-link nav items are dead buttons in the mobile menu ✓
- **Where:** `components/navbar/MobileNavbar/MobileMenuItem.tsx:31-90` vs `components/navbar/DesktopNavbar/index.tsx:33-35`
- **What:** Desktop branches on the column type: `if (!isMultiColumn && column.link) return <HawkLinkComponent link={column.link} …/>;`. Mobile always renders a `<button>` that toggles `showOptions`, and its panel is populated from `data.dropdown?.links?.dropdownNavLink` — `undefined` for a non-multi-column entry, so `visibleLinks?.map(...)` renders nothing. The button label is `data.link?.label`, so the item *looks* right. `payload/globals/Header/config.ts:42`, `:57` confirms `isMultiColumn: false` columns carry a `link` and no `dropdown`.
- **Impact:** On phones, any top-level nav entry configured as a direct link is a button that expands an empty panel and goes nowhere. Not strictly i18n, but it breaks navigation identically in both locales and it is the most severe navigation defect found.
- **Fix:** Mirror the desktop branch — render `<HawkLinkComponent link={data.link} />` when `!isMultiColumn`.

### [Medium] I18N-3 — Date formatting hardcoded to the wrong locale in three shipped components
- **Where:** `payload/blocks/LatestNewsBlock/LatestNewsBlockView.tsx:29` (`toLocaleDateString('en-US', …)`), `AgendaBlock/AgendaBlockView.tsx:51`, `:70`, `:107`, `:115` (`'pt-PT'`), `components/Crowdfunding/CrowdfundingUpdates.tsx:11` (`'pt-PT'`)
- **What:** `i18n/settings.ts:32-36` provides a `toIntlLocale()` helper for exactly this, and `components/news/single/NewsSingleHero.tsx:21` and `OrganizationContributionsTable.tsx:27` both use it correctly.
- **Impact:** Latest News shows "September 14, 2026" on Portuguese pages; Agenda and Crowdfunding updates show "set." to English visitors. Both are page-builder blocks, so they appear on arbitrary pages in both locales.
- **Fix:** Thread `lng` down and call `toLocaleDateString(toIntlLocale(lng), …)`.

### [Medium] I18N-4 — Member-project dates no longer render at all ✓ **[regression introduced 2026-09-15]**
- **Where:** `components/members-corner/MembersShowcase.tsx:15-18`
- **What:** The hardcoded `DD/MM/YYYY` formatter was replaced with a stub:
  ```ts
  const formatDate = (iso: string) => {
    // TODO
    return '';
  };
  ```
  The explanatory comment above it (about avoiding a hydration mismatch) is still there, now describing a function that formats nothing. `eslint` flags the dead parameter — `15:21 warning 'iso' is defined but never used` — which is one of the two files failing the lint gate (QUA-30).
- **Why it matters:** The original finding was that `05/11/2026` is ambiguous between locales. Returning an empty string resolves the ambiguity by deleting the information: every member project in the showcase now displays a blank where its date was. That is a worse outcome than the bug, and it is shipped rather than parked behind a flag.
- **Fix:** `new Intl.DateTimeFormat(toIntlLocale(lng), { timeZone: 'UTC', day: '2-digit', month: 'short', year: 'numeric' }).format(new Date(iso))`. The explicit `timeZone` makes it deterministic across server and client, so the hydration concern the comment describes is handled without dropping either the locale or the date.

### [Medium] I18N-5 — EUR amounts on the crowdfunding pages bypass currency formatting
- **Where:** `components/Crowdfunding/CrowdfundingHero.tsx:143`, `:162`, `:170` and `CrowdfundingTransparency.tsx:71`, `:89`, `:101`
- **What:** All six use `` € {(raisedAmount ?? 0).toLocaleString(lng === 'pt' ? 'pt-PT' : 'en-GB')} ``, while `lib/utils/currency.ts:4-12` already provides `style: 'currency', currency: 'EUR'` with fixed fraction digits.
- **Impact:** A hardcoded leading `€ ` is wrong for `pt-PT`, which places the symbol after the amount ("38 064,00 €"), and cents are dropped entirely — `38064.5` renders as "38 064" on a public financial-transparency widget.
- **Fix:** Replace all six with `formatCurrency(amount, lng)`.

### [Medium] I18N-6 — Hardcoded English UI strings in shipped components
- **Where:** `payload/blocks/MapLocationBlock/Component.tsx:52`, `:61`, `:73`, `:85` (`<h3>Address</h3>`, `Phone`, `Email`, `Hours`, plus `aria-label='map'`); `DonationProgressBlock/Component.tsx:144`, `:150` (`Raised`, `Goal`); `HeroSlideshowBlock/Component.tsx:169`, `:176`, `:194`; `TestimonialBlock/Component.tsx:215`, `:227`; `ProjectTestimonialBlock/Component.tsx:144`, `:151`; `components/agenda/AgendaCalendar.tsx:296`, `:308`, `:313`, `:352`. CMS-field English defaults: `NewsletterSignupBlock/Component.tsx:15` (`'Subscribe'`), `LatestNewsBlock/Component.tsx:14` (`'Read more'`), `UpcomingHawkEventBlock/Component.tsx:22` (`'Learn more'`), `CampaignCountdownBlock/Component.tsx:29` (`'Campaign Ended'`), `SimpleGallery/Component.tsx:17` (`'Beautiful Interiors.'`)
- **Impact:** Portuguese users — the *default* locale — see English headings on the contact/map block and the donation progress bar, and Portuguese screen-reader users hear English announcements for every carousel and calendar control. The block defaults are the worst case: an editor who leaves `buttonText` empty ships an English "Subscribe" button on a Portuguese page, and `SimpleGallery` defaults to placeholder copy.
- **Fix:** Route these through `useTranslation`/`getServerTranslation` (the `common` namespace already has `a11y.*` keys), and make the CMS text fields `required` rather than falling back to an English literal.

### [Medium] I18N-7 — Media `alt` text is not localized ✓
- **Where:** `payload/collections/Media/index.ts:68-79`
- **What:** The field is `required: true` (good) but carries no `localized: true`, unlike 187 other localized field declarations across `payload/`. Note the *label* is bilingual; the value is not.
- **Impact:** Every uploaded image carries one alt string shared by both locales, so half the site's screen-reader users get alt text in a language they may not read. This is the single highest-leverage combined a11y + i18n fix available.
- **Fix:** Add `localized: true`. `payload.config.ts:166-169` sets `fallback: true` with `fallbackLocale: 'pt'`, so existing values survive as the PT value.

### [Low] I18N-9 — `Intl` called with no locale in the Instagram feed
- **Where:** `components/socials/InstagramFeed/InstagramGrid.tsx:20`, `:26`
- **What:** `{post.likeCount.toLocaleString()}` with no locale, so the runtime default applies.
- **Impact:** `1,234` instead of `1.234` on Portuguese pages, plus a potential hydration mismatch if server and browser defaults differ.
- **Fix:** Pass `toIntlLocale(lng)`.

---

## Accessibility

### [High] A11Y-1 — Form field boundaries are at 1.21:1 contrast (WCAG 1.4.11 requires 3:1)
- **Where:** `components/utils/Input/Input.tsx:49` and `components/utils/TextArea/TextArea.tsx` — `border-b-bege-dark … border-b-2`; token at `app/globals.css:12` (`--color-bege-dark: #fae7d0`)
- **What:** Relative luminance of `#fae7d0` computes to 0.8204, giving **1.21:1** against white and **1.16:1** against `--color-bege-light` (`#fef9f6`), the page background on several form pages. The bottom border is the *only* visual boundary — no fill, no other borders.
- **Impact:** Low-vision users cannot locate the input at all. This is the shared input used across the site, including the member-project submission form. The same defect appears with different tokens in the donation flow: `components/contribute/DonationWidget/DetailsStep.tsx:61`, `:78`, `:95`, `:105`, `:138` use `border border-[#ddd]` = **1.36:1**; and `app/globals.css:376` sets `--border: oklch(0.922 0 0)` = **1.26:1** against `--background`, affecting every shadcn primitive using `border-border`.
- **Fix:** Use a border meeting 3:1 against the page background — `--color-disabled` (`#5b5b5b`) computes to 6.79:1 against white and clears the threshold comfortably — and keep `bege-dark` as a decorative accent only.

### [High] A11Y-2 — The donation form's only error handling is a disabled button (WCAG 3.3.1, Level A)
- **Where:** `components/contribute/DonationWidget/DetailsStep.tsx:38-42`, `:153-160`
- **What:** `const isValid = name.trim().length > 0 && email.trim().length > 0 && email.includes('@');` gates `handleNext`, and the button is `disabled={!isValid}`. There is no error text anywhere in the file, no `aria-invalid`, no `aria-describedby`, no live region. `components/utils/Input/Input.tsx:31-33`, `:67-77` does all of this correctly, as does `NewsletterSignupBlock/Component.tsx:82-83`, `:105-109`.
- **Impact:** A user who types a malformed email gets a permanently greyed-out Continue button and no explanation. Because `disabled` removes the button from the tab order, a screen-reader user tabbing through never encounters it and has no way to discover anything is wrong — on the organisation's donation flow.
- **Fix:** Keep the button enabled, validate on submit, set `aria-invalid` + `aria-describedby` on the offending field, render the message in `role='alert'`, and move focus to the first invalid field.

### [High] A11Y-3 — Auto-advancing hero slideshow with no pause control (WCAG 2.2.2, Level A)
- **Where:** `payload/blocks/HeroSlideshowBlock/Component.tsx:36-37`, `:63-68`
- **What:** `autoplay = true`, `autoplayInterval = 5000`, driving `setInterval(nextSlide, …)`. The only controls rendered are prev/next (`:166-179`) and dots (`:186-195`). No pause/stop button, no pause on hover or focus, and no `prefers-reduced-motion` check anywhere in the file.
- **Impact:** SC 2.2.2 is Level A and requires a mechanism to pause auto-updating content that starts automatically and lasts more than five seconds. Autoplay defaults to on, so this ships on by default. For a public-funded NGO in the EU this is an EN 301 549 / Web Accessibility Directive exposure, not a polish item.
- **Fix:** Add a pause/play toggle, stop the interval on hover and focus-within, and disable autoplay under `@media (prefers-reduced-motion: reduce)`.

### [High] A11Y-4 — The crowdfunding mobile menu keeps its links focusable while closed, and the toggle has no `aria-expanded`
- **Where:** `components/Crowdfunding/CrowdfundingNavbar.tsx:67-71`, `:94-99`
- **What:** The toggle has `aria-label` but no `aria-expanded`/`aria-controls`. The panel is hidden with `max-h-0` + `overflow-hidden`.
- **Impact:** Clipping is visual only — the four nav anchors and the back-to-site link remain in the DOM, the accessibility tree and the tab order. Keyboard users tab into invisible links; screen-reader users hear a menu sighted users cannot see; the toggle never announces its state.
- **Fix:** Add `aria-expanded`/`aria-controls`, and gate the panel with `hidden={!mobileOpen}` or `inert` rather than `max-h-0`.

### [Medium] A11Y-5 — The main site's hamburger has no `aria-expanded`, and focus is not restored on close
- **Where:** `components/navbar/Navbar.tsx:69-76`; close path at `components/navbar/MobileNavbar/index.tsx:58-66`
- **What:** The trigger has `aria-label` only. Checked against the siblings: `DesktopNavbar/index.tsx:45-47` supplies `aria-expanded` + `aria-haspopup` + `aria-controls`, and `MobileMenuItem.tsx:33-34` supplies them too — this one trigger is the gap. Two smaller gaps in the same flow: focus is moved *into* the dialog on open but never restored to the hamburger on close, and the background page is neither scroll-locked nor `inert` while `role='dialog' aria-modal='true'` is up, so the modal claim is not enforced.
- **Fix:** Add `aria-expanded`/`aria-controls`; store the trigger and `.focus()` it on close; add `inert` / `overflow:hidden` to the page while open.

### [Medium] A11Y-6 — Hidden slideshow slides are keyboard-reachable and announced
- **Where:** `payload/blocks/HeroSlideshowBlock/Component.tsx:90-96`
- **What:** Inactive slides get `opacity-0` only, and each slide contains a heading, a paragraph and CTA anchors (`:126`, `:132`, `:146-152`).
- **Impact:** Tab order walks through every off-screen slide's CTA, sending focus to invisible content; screen readers read all slides as one block. There is also no `aria-live` region announcing changes and no `aria-current` on the dots, so a non-visual user cannot tell which slide is active.
- **Fix:** `aria-hidden='true'` + `inert` on inactive slides, `aria-current='true'` on the active dot, and an `aria-live='polite'` wrapper that is `off` while autoplay runs. Fixes SEO-8's DOM half too.

### [Medium] A11Y-7 — KA-Sport pill: white text at 3.68:1
- **Where:** `app/[lng]/(org)/erasmus/key-action/page.tsx:113-118` with `config.ts:150` (`color: '#d4660a'`)
- **What:** L(#d4660a) = 0.2353 → **3.68:1** against white, below the 4.5:1 AA threshold. At `text-[0.68rem]` (≈10.9 px) it is nowhere near the large-text exemption. The other four colors in the same config all pass (6.37, 8.10, 5.44, 8.57), so this is a single outlier, not a palette problem.
- **Fix:** Darken to roughly `#a34f08` (≈4.9:1), or use a dark foreground on a light tint.

### [Medium] A11Y-8 — Meaningless English alt-text fallbacks when the CMS alt field is empty
- **Where:** `SimpleGallery/Component.tsx:64` (`'placeholder'`), `Hero/Component.tsx:40` (`'Hero'`), `MultiRowImage/Component.tsx:101`, `BentoGridBlock/Component.tsx:150` (`'Bento Grid Item 3'`), `ProjectTestimonialBlock/Component.tsx:92`, `:124`, `:197`, `curator/[slug]/page.tsx:100`
- **Impact:** A screen reader announces "placeholder", "Bento Grid Item 3", "Multi Row Image -2" — worse than an empty alt, which at least lets the image be skipped. All English on a PT-default site. The good pattern exists elsewhere: `SingleProjectResults.tsx:34` uses `alt={img.alt || t('a11y.resultsAlt')}` and `AgendaBlockView.tsx:162` falls back to `event.heading`.
- **Fix:** Fall back to a meaningful sibling field (title/heading/name), or to `''` for genuinely decorative backgrounds (`Hero/Component.tsx:40` is one) — never to an invented English label.

### [Medium] A11Y-9 — No skip-to-content link on the crowdfunding and gaming sub-sites
- **Where:** `app/[lng]/(crowdfunding)/layout.tsx:99` and `(gaming)/layout.tsx:104` — neither `<main>` has an `id`, and neither layout renders a skip link. The org layout does it correctly at `(org)/layout.tsx:44`, `:68-79`, `:91`.
- **Impact:** WCAG 2.4.1 (Bypass Blocks, Level A). Keyboard users must tab the full navbar on every page of both sub-sites.
- **Fix:** Extract `SkipToContent` and mount it in all three layouts; add `id='main-content'` to both `<main>` elements.

### [Medium] A11Y-10 — `components/ui/input.tsx` drops the `name` attribute and points `aria-labelledby` at a non-existent id
- **Where:** `components/ui/input.tsx:8-21`
- **What:** Three defects at once: `name` is destructured out and never forwarded to the `<input>`; the `<label>` has an empty body and an `htmlFor` pointing at an id nothing sets; and `aria-labelledby={name}` references an element that does not exist, which per ARIA yields an **empty accessible name** and — unlike a missing attribute — actively suppresses any other naming source.
- **Impact:** On severity: a grep of every import shows this is used **only** by six files under `stories/`, so live impact today is zero. The risk is that it is the canonical `@/components/ui/input` the next developer reaches for. The good primitive is `components/utils/Input/Input.tsx`, whose comment at `:27-30` records that exactly this `aria-labelledby={name}` bug was already fixed once.
- **Fix:** Forward `name`, set `id`, render a real label — or delete the file and re-export `components/utils/Input/Input.tsx`.

### [Low] A11Y-11 — Calendar day buttons announce a raw ISO string and no state
- **Where:** `components/agenda/AgendaCalendar.tsx:343-352`
- **What:** `` aria-label={`Date: ${dateKey}`} `` where `dateKey` is `YYYY-MM-DD`, on a button with no `aria-pressed` despite `isSelected` driving a visual change.
- **Impact:** Screen readers read "Date: 2026-09-14" in English, never mention that the day has events (the coloured dots are the only indicator), and never expose selected state.
- **Fix:** Build the label from the localized date plus an event count, and add `aria-pressed={isSelected}`.

### [Low] A11Y-12 — The newsletter success message steals focus into nothing
- **Where:** `payload/blocks/NewsletterSignupBlock/Component.tsx:69`, `:95-103`
- **What:** The whole form — including the focused submit button — is unmounted on success and replaced by a `role='status'` div.
- **Impact:** Focus drops to `<body>`. The status will be announced, but the keyboard user's position is lost mid-page, and the 3-second reset at `:40-43` swaps it back without warning.
- **Fix:** Keep the form mounted and disabled, render the status alongside, and move focus to it.

---

## Verified good

Not a courtesy list — these are things that were specifically checked and found correct, several of which are the exact defects the findings above would predict.

**Type safety and hygiene**
- Zero `@ts-ignore`, `@ts-expect-error` and `@ts-nocheck` in the entire tree. Exactly **seven** explicit `any`, five of them unavoidable image-module declarations with a scoped eslint-disable, the other two commented. No `as unknown as` laundering anywhere.
- `tsconfig.json:7` is `strict: true` with no escape hatches, and `next.config.ts` contains **neither** `typescript.ignoreBuildErrors` nor `eslint.ignoreDuringBuilds`.
- **Zero** `console.log`/`debug`/`info` anywhere, including stories and configs. Two TODOs in 673 files. Only two `JSON.parse` calls, both in tests, both on values the test constructed.
- Both non-null assertions in `lib/google-drive/supportersWorkbook.ts:122-126` are correctly guarded, and so is `payload/components/Media/resolve.ts:28` — checked specifically before writing them off.

**Structure**
- The block system is perfectly consistent: all 45 folders have `config.ts` + `Component.tsx`, and the 45 config slugs and the 45 renderer-registry keys are an **exact set match**, verified with `comm` — zero orphans in either direction.
- No unreferenced components. The one candidate (`components/payload/ContributionSelect.tsx`) is referenced by string path from the collection config and `importMap.js`.
- `ONBOARDING.md`, `.claude/*` rules and per-file reasoning comments are unusually thorough; several findings above exist *because* the reasoning was written down and can be compared against the code.

**Security**
- The EasyPay webhook fails **closed** when its secret is unset, uses `timingSafeEqual` after a length pre-check, and deliberately takes the secret from a header rather than a query parameter because nginx logs full URLs — all explained inline and all three behaviours tested.
- Mass-assignment is closed on member submissions at **four** independent layers: the route forces `is_confirmed: false`, a field `beforeValidate` hook, field access `create: () => false`, and a collection `beforeValidate` hook.
- `getClientIp` (`utils/rateLimit.ts:84-88`) takes the *last* `X-Forwarded-For` entry, matching nginx's `$proxy_add_x_forwarded_for`, so a client-supplied header cannot rotate past the limiter — with the previous vulnerability documented in the comment. The limiter is applied to all five public API routes and the geocode endpoint (per-user *and* global buckets).
- All nine preview routes are server-side auth-gated with `payload.auth({ headers })` → `notFound()`, **and** return `robots: 'noindex, nofollow'`, **and** are disallowed in `app/robots.ts:19`.
- Upload MIME allowlists are explicit and raster-only; `Media/index.ts:54` excludes `image/svg+xml` with the XSS rationale recorded.
- Newsletter signup does not leak subscriber existence — duplicate-key errors are swallowed so re-subscribing is indistinguishable from a fresh signup.
- Sentry `sendDefaultPii: false` in all three configs with the lawful-basis reasoning written down; session replay masks all text and blocks media; `beforeSend` suppresses build-phase events; `denyUrls`/`ignoreErrors` are thorough and annotated.
- `payload.config.ts:62-92` asserts every required env var at boot, with the Gmail OAuth set asserted only in production.
- TOTP is `forceSetup` in production. No `eval`/`new Function` anywhere. The only `process.env.*` reaching a client bundle is the Carto key.
- Rich text renders through Payload's official JSX converters, never raw HTML.

**Performance**
- Collection-level tag invalidation is systematic and correct for **nine of ten** collections, with tags matching real slugs and `revalidateTag(tag, 'max')` used consistently.
- `<Suspense>` boundaries are placed *above* the `params` await via non-async page shells on every list and detail route — a subtle detail the code gets right deliberately and documents.
- Independent queries are consistently batched with `Promise.all`.
- Images are fully centralised on one `next/image` wrapper with Cloudinary LQIP blur, `fetchPriority` on preloaded images and correct `fill` handling — and **zero raw `<img>` tags anywhere in the app**. `sizes` is supplied at ~20 responsive call sites, and the decision *not* to default it is correct and explained.
- `next.config.ts:52-70`: AVIF/WebP, tight `deviceSizes`, restricted `qualities`, 12-hour `minimumCacheTTL`, explicit `remotePatterns`.
- Fonts via `next/font/google` with `display: 'swap'` and subsets; analytics via `next/script strategy="afterInteractive"`; no render-blocking external CSS.
- Leaflet is correctly kept off the main bundle behind `dynamic(…, { ssr: false })`; `exceljs` and `@googleapis/drive` appear only in server-only modules.
- Drafts are explicitly excluded from the cache path (`helpers.ts:92-94`), and all preview routes correctly use `connection()`.
- nginx static/asset caching is thorough — `proxy_cache_lock`, `background_update`, `use_stale`, and a `/_next/image` location that query-string-keys each variant.

**CI, deploy and testing**
- The `on:` block is **structurally correct** — verified by parsing the YAML: `push` and `pull_request` are true siblings, so PRs do run `check`. (This was worth checking; an indentation bug here silently disables the gate.)
- The payment route tests are substantive, not smoke tests: 21 cases for `/api/donate`, 28 for the webhook (all four auth rejection paths, idempotence of the *update* path, 200-on-error), 21 for `/api/subscription`.
- `db-backup.yml` is the better-engineered of the two workflows: `permissions: {}`, a concurrency group, a 30-minute timeout, `set -euo pipefail`, `command -v` preconditions, `:?missing` guards on every secret, a cleanup `trap`, explicit failure checks and retention pruning.
- The Discord notify step builds its JSON with `jq -n --arg`, so a commit message containing quotes or newlines cannot break the payload; uses `curl --fail-with-body`; exits 0 cleanly when the webhook secret is absent.
- The deploy writes `.env` with `chmod 600` and unsets the injected PAT from the server's global gitconfig after the pull.
- `ecosystem.config.cjs` explains why it runs the `next` binary directly rather than wrapping `npm start` (signal forwarding, accurate exit codes) and why `cwd` is absolute (the `ENOENT /root/package.json` bug).
- Dependency versioning is deliberate: `~` pins exactly the framework-coupled packages, `^` elsewhere; `pnpm-workspace.yaml` sets `minimumReleaseAge: 2880` as a supply-chain delay plus an explicit `allowBuilds` allow-list.
- App Router error boundaries exist and are non-trivial — `global-error.tsx`, `app/not-found.tsx`, a localized `(org)/error.tsx` with `reset()` and Sentry capture, plus `loading.tsx` in all three groups. `instrumentation.ts` exports `onRequestError`.
- The `RichText` unknown-node handler reports unrecognised Lexical types to Sentry in production rather than crashing or silently dropping content.
- The Google Drive supporters import is transactionally careful — `onNewRows` must resolve before any row is marked imported, and a failed write-back after a successful Payload write is explicitly flagged as a duplicate risk.

**i18n and accessibility**
- **Translation key parity is perfect**: all 18 namespace pairs flattened and diffed — 0 keys missing in `pt`, 0 missing in `en`, across 900+ keys. No user-facing raw keys found.
- `utils/metadata.ts:83-171` is a genuinely well-built metadata factory: one normalised `path` drives canonical, hreflang and `og:url`, so self-referencing hreflang cannot disagree with canonical; `metadataBase` set; `x-default` → `/pt` matches `fallbackLng`; OG image dimensions and alt fall back through a real chain.
- `components/utils/Input/Input.tsx` is a reference implementation — real `<label htmlFor>`, `aria-describedby` composed from hint + error ids, `aria-invalid`, `<small role='alert'>`, and a comment explaining why `aria-label` must *not* be added.
- `SubmitProjectForm.tsx` uses react-hook-form with per-field errors, a `role='alert'` submit error, a `role='status'` success panel, and an explicitly-explained label association for the Radix combobox.
- `MobileNavbar/index.tsx` implements a real focus trap, Escape to close, `role='dialog' aria-modal='true'`, and moves focus to the close button on open.
- `LanguageSwitcher` uses real `<Link>` elements with `hrefLang`, `lang`, `aria-current` and a translated `aria-label` — with a comment recording that it used to be a keyboard-inaccessible `div onClick`.
- `Accordion.tsx` is a correct hand-rolled disclosure: heading > button > `aria-expanded`/`aria-controls`, `role='region'` panel, `hidden` (not opacity) when closed.
- 15 of 27 `components/ui/*` files are Radix-backed, so dialogs, tabs, selects and accordions get correct roles and keyboard handling for free. `components/ui/carousel.tsx` adds `aria-roledescription`, arrow-key handling, translated `sr-only` labels — and no autoplay.
- Maps have text alternatives: `SingleProjectTravelMap.tsx:234-265` renders every leg and the total as text; `MapLocationBlock` renders address, phone, email and hours beside the iframe.
- Icon-only buttons are labelled — a scripted sweep for icon-only `<button>`s with no accessible name returned exactly one hit, which proved to be a false positive.
- `withHandleInternalization.ts:27-28` uses whole-segment locale matching, with a comment on the `/ptx/foo` and `/entertainment` bugs the loose `startsWith` caused.
- `payload.config.ts:163-170` sets `fallback: true` with `fallbackLocale: 'pt'`, so a missing English translation renders Portuguese rather than an empty element.

---

## Fixed before this audit (carried over from the 2026-09-10 pass)

For fixes made *since* this audit, see **Fixed and verified closed** near the top. The items below are conditions the previous, 2026-09-10 audit reported that the tree had already stopped exhibiting when this pass began — checked against the code, not against that document:

- **The `deploy.yml` trigger indentation bug is gone.** `pull_request` is now a proper sibling of `push` under `on:`, confirmed by parsing the YAML. The `check` job does run on PRs.
- **The no-op JSON-LD escape is fixed.** `components/seo/JsonLd.tsx` now uses `.replace(/</g, '\\u003c')`, which is a real escape — verified character by character. Four of the five builders apply it (SEC-14 covers the fifth, which is dead code).
- **The 28 `EventPage.tsx` type errors are gone.** The `Record<string, unknown>` migration appears complete. (The tree typechecked cleanly on 2026-09-14; it no longer does, for an unrelated reason introduced since — QUA-32.)
- **`/api/member-projects` no longer returns 200 on failure.**
- **The sitemap's `_status` handling was extended** to `news` and `hawk_projects` — `pages` is the one that was missed (SEO-2).
- **`MAIN_PAGE_CACHE_TAG` is wired up** and the homepage data query is cached (though the route still opts out of prerendering — PERF-7).

---

## What was not verified

Stated plainly, because an audit that does not say what it could not check is not much use.

- **Nothing was executed against a running application.** `tsc`, `eslint` and `prettier` were run for real (results in the toolchain table), but no server was started, no HTTP request was issued and no database was queried. Every *finding* is from static reading except QUA-30. In particular, SEC-2 and SEO-2 rest on Payload's documented `_status` semantics rather than an observed response, and SEO-1's status code was inferred from the absence of `notFound()`.
- **`vitest run` and `next build` could not be run.** `node_modules` was installed on macOS and the shell available here is linux-aarch64, so rolldown's native binding does not resolve. The test suite's actual pass/fail state is unknown — which matters, because QUA-12 is independent evidence that CI may not be running it meaningfully either. **Run both locally before acting on this report.**
- **Whether the `map.tsx` lint errors predate the uncommitted changes.** `git diff --stat` shows that file is modified; a clean checkout of `HEAD` was not linted separately, so QUA-30 does not establish whether the errors are new or pre-existing.
- **Payload internals.** `node_modules` was not readable, so the following could not be confirmed from source: that the default for an unspecified access key is `Boolean(user)` rather than public — this is load-bearing for SEC-6, rated Medium on the assumption that the fallback is "any account"; if the fallback were actually public, it becomes Critical, since the homepage global would then be rewritable without authentication. Worth confirming directly. Also unverified: that `beforeLogin` runs only after successful password verification (SEC-8); whether field-level `access` on an unnamed `collapsible` is honoured at all; the default for `jobs.access.run`, which `payload/jobs/index.ts` leaves unset while exposing `/api/payload-jobs/run`; and whether `graphQL.disablePlaygroundInProduction` still defaults to `true`.
- **Whether the production `PAYLOAD_SECRET` equals the 7-character dev value** — it comes from a GitHub secret. Likewise whether branch protection makes `check` a required status check; if it is not, the entire CI story reduces to "runs after the merge, then deploys".
- **Bundle sizes.** No build or analyzer run was possible. PERF-3 and PERF-4 are structural certainties from the import graph; any KB figures would be estimates, so none are quoted.
- **Runtime infrastructure.** Whether `limit_req_zone mylimit` and `proxy_cache_path assets_cache` are declared in the host `nginx.conf`; whether `mongod` binds `127.0.0.1` or `0.0.0.0`; whether the deployed nginx config matches the one in the repo (`deploy.yml:105-109` notes it can exist untracked on the server, which is its own drift risk).
- **Mongo indexes.** The `where` clauses on `date`/`startDate`/`slug`/`is_confirmed` were read, but no index definitions exist in the Payload configs and the database was not inspected. Unindexed range scans on `hawk_events` and `contributions` are plausible but unconfirmed.
- **Rendered contrast in composited contexts.** Ratios were computed only where literal hex/oklch values could be read against a literal background. Combinations involving `opacity`, `color-mix()` or `backdrop-blur` were estimated by alpha compositing, and text over editor-controlled photographic hero backgrounds cannot be computed statically at all.
- **Screen-reader behaviour.** No NVDA/VoiceOver/axe run; all a11y findings are from source inspection.
- **Roughly 30 of the 45 page-builder blocks** were not individually opened. The heading, image, form, carousel and map blocks were read in depth, plus every block matching the hardcoded-string and `alt=` sweeps. `StatsBlock`, `TimelineBlock`, `PricingTableBlock`, `OfferCatalogBlock`, `StepsBlock`, `SocialProofBlock`, `ResourceDownloadBlock`, `QuoteHighlightBlock`, `GrowthVisionBlock`, `WhyHereWhyNowBlock` and others may hold issues the greps did not pattern-match.
- **`components/ui/map.tsx`** (1,228 lines) was checked only for `aria-*`/`role`/`alt` attributes. Leaflet keyboard navigation of the map canvas, and whether markers are reachable by keyboard, were not assessed.
- **Dead translation keys.** A static pass flagged 483 unreferenced leaf keys, but spot-checking showed most are composed dynamically (`` t(`roles.${role}`) ``, `` t(`languages.${project.language}`) ``). Only about eight were confirmed genuinely unreferenced — too few to file as a finding.
